# UPDATE v2.9w — 최종 재검토 (2026-09-14)

v2.9v 까지 포함해요. **SQL 없어요.** 코드 변경은 한 줄(아래 ①)뿐이고 나머지는 검증 기록이에요.

---

## 재검토 방법

기억이 아니라 **코드와 숫자로** 다시 확인했어요.

1. 원본 v2.9f 와 전체 diff → 변경 파일 71개
2. 날짜 공식을 실제 기관 데이터에 다시 대조
3. **제가 바꾼 테스트 기대값 전부**를 뽑아 근거 확인 ← 제일 중요
4. 중괄호 깊이를 추적해 구조 변경이 맞는지 확인
5. 안전장치를 없앤 자리가 있는지 확인

---

## ① 한 군데 보강했어요

**비행경력증명서 주간 자동계산** — 저장 시점 폴백을 넣었어요.

```ts
day: numOrUndef(form.get('conditionDay')) ?? dayHint?.day,
```

v2.9n 에서 "주간이 비면 저장 차단"을 없애고 자동 채움으로 바꿨는데, 자동 채움이
`useEffect` 라 **효과가 돌기 전에 저장하면** 이론상 비어 나갈 수 있었어요.
발동 조건은 `dayHint` 와 같아서 값이 달라질 일은 없어요.

옛 검증이 막던 범위와 새 자동 채움이 덮는 범위가 **정확히 같다**는 건 확인했어요
(둘 다 `주간 비었음 + 총·야간 있음 + day > 0`). 폴백은 순수한 여유분이에요.

---

## ② 바꾼 테스트 기대값 — 3개뿐, 전부 근거 있음

이게 제일 위험한 부분이에요. 잘못 바꾸면 **버그를 정답으로 박게** 되니까요.

| 기대값 | 옛 → 새 | 근거 |
|---|---|---|
| EPTA 기준일 역산 | `2025-12-01` → `2025-12-02` | **TS 영어등급조회 실제 기록** |
| EPTA 5등급 만료 | `2032-03-17` → `2032-03-16` | 같은 조항 유추 · **실제 데이터 없음** ⚠️ |
| `licenceCode` 없으면 `[]` | `[]` → `['epta']` | 그 자체가 버그(단독 항공영어 무시) |

나머지 테스트는 **추가만** 했고 기존 기대값을 건드리지 않았어요.

---

## ③ 날짜 공식 재검증

```
항공영어 (TS 실제 3건)
  2021-06-10 → 옛 2024-06-10 ❌ | 새 2024-06-09 ✅ | 실제 2024-06-09
  2024-06-10 → 옛 2027-06-10 ❌ | 새 2027-06-09 ✅ | 실제 2027-06-09
  2025-12-02 → 옛 2028-12-02 ❌ | 새 2028-12-01 ✅ | 실제 2028-12-01

통신보안교육 (전파진흥원 실제 1건)
  2022-03-08 → 옛 2027-03-08 ❌ | 새 2027-03-07 ✅ | 실제 2027-03-07

윤년: 2024-02-29 +3년−1일 = 2027-02-28
  → 민법 제160조③("최종의 월에 해당일이 없으면 그 월의 말일") 과 일치
월말: 2025-01-31 +3년−1일 = 2028-01-30  ✅
```

**통신보안교육 판정 경계**(`<=` → `<`)는 **표시 정정이고 판정 회귀가 없다**는 걸
날짜 4개로 확인했어요. 옛 조합도 boolean 은 맞았어요(오차 두 개가 상쇄).
실제로 고친 효과는 **기준일**이에요.

```
발급일 기준 2017-11-24 → 2022-11-23 → "지남" ❌ 잘못된 경고
최종교육일  2022-03-08 → 2027-03-07 → "유효" ✅
```

---

## ④ 구조 변경 검증 (조기 반환 → 블록 감싸기)

중괄호 깊이를 추적했어요.

```
125  if (otherLicences.length > 0) {  … 144 return out … 145 }
151  if (code) {                      … 259 }
261  // ── 항공영어 ──                  ← if(code) 바깥. 본체가 아니어도 항상 돈다
286  return out
```

의도대로예요. 제안 **순서**도 원래대로 유지돼요(순서 회귀는 기존 테스트 3건이 잡아 줬고
구조를 다시 잡아 해결했어요).

---

## ⑤ 표시만 바꾼 것들 — 계산 무관 확인

| 변경 | 확인 |
|---|---|
| 이월 기록 배지 제거 | `deriveBadges` 는 `EntryList` 전용 — 커런시·응시경력·PDF 무관 ✅ |
| 서명 표 `{}` 정리 | 값 자체는 안 바꿈. `0` 은 `0` 으로 표시(테스트 고정) ✅ |
| 조종교육증명 등급 표시 | 저장된 이름에서 읽기만 함 ✅ |
| 서명 이미지 서버화 | PDF 는 `instructorSignature ? '전자서명' : ''` — **객체 존재만** 봄 ✅ |

서명 변경 후에도 `hasSignatureHistory` 게이트가 `signedRequestId \|\| instructorSignature`
를 보므로, 이미지를 빼도 객체(이름·uuid·시각)는 남아 불러오기가 안 깨져요.

---

## ⑥ 아직 검증 못 한 것 — 솔직하게

| 항목 | 상태 |
|---|---|
| **AI 프롬프트 변경 전부** | 컨테이너에 API 키가 없어 **실제 호출 미검증**. 배포 후 확인 필요 |
| **신체검사 종류 버그가 실제로 발현했는지** | 계약이 깨진 건 원본으로 확인. 다만 모델이 지시를 어기고 문자열로 답했다면 옛 코드도 동작했을 수 있음. 새 코드는 **둘 다 받아** 어느 쪽이든 안전 |
| **통신보안교육 "늦은 쪽" 기준** | 전파법 제30조·시행규칙 제7조에서 **유추**한 것. 조문 원문은 안 읽음 ⚠️ |
| **통신보안교육 −1일** | 실제 데이터 **1건**뿐(항공영어는 3건) ⚠️ |
| **EPTA 5등급(6년)·6등급** | 실제 데이터 없음. 같은 조항에서 유추 |
| **EPTA 제99조③ 2호(갱신)** | 미구현. 갱신자는 기준일 칸에 "기존 만료일 + 1일"을 직접 넣어야 함 |
| **경량항공기 자격증** | 실물 서식으로 미확인 |
| **E2E 20건** | 브라우저가 없어 실행 못 함(목록만 확인). 매일 05:00 자동 실행으로 확인됨 |

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    178 통과 (16 파일)
npm run build                     통과
npx playwright test --list        20건
node --check netlify/functions/*  전체 통과
```

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. **Netlify 함수가 바뀌었어요** — 배포 뒤 AI 읽기 확인
3. 우선순위 확인
   - **항공신체검사증명서** → 종류(1·2·3종) 자동 선택 ← 제일 중요
   - 자격증명서 → 조종교육증명 **4개**
   - 비행경력증명서 → 총·야간 넣으면 **주간 자동**
