# UPDATE v36 — 서명 요청함 성능 · 날짜 필터 (2026-09-04)

## 왜 느렸고, 왜 쌓일수록 무거워졌나
서명 요청함은 **요청 1건마다 댓글을 따로 조회**했습니다(N+1). 요청 50건이면 요청 51번. 게다가 화면에 보이는 5건만 조회하므로
**안 보이는 페이지의 상태는 아예 모른 채 전부 "대기중"으로** 셌습니다. 승인 교관 명단도 신청서마다 댓글을 따로 조회했습니다.

## 수정
- 배치 댓글 조회 API 추가(`/public/boards/comments/batch?post_ids=…`, 한 번에 500건). `useCommentsBatch` 훅.
- 서명 요청함: 목록 1번 + 댓글 배치 1번 = **요청 2번**으로 모든 항목 상태 판정. 서명 후에는 배치만 재조회.
- 승인 교관 명단 계산도 배치 1번으로(기존: 신청서 수만큼).
- 카드 컴포넌트는 더 이상 스스로 조회하지 않고 상위에서 댓글을 받습니다.

## 날짜 필터 · 페이지
- 탭 옆에 **비행 일자** 선택(제목의 날짜 기준). "전체"로 해제.
- 페이지당 5건, 이전/다음 이동(기존 기능 유지). 필터를 바꾸면 1페이지로.
