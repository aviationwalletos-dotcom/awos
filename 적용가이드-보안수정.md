# AWOS 보안 수정 적용 가이드 (2026-08-29)

이 압축파일에는 분석 리포트의 🔴1(서명 위조), 🔴3(첨부 공개), 🔴4(구 백엔드 잔재), 🟠6(키 중복), 🟠7(CSP), 🟠8(테스트), 🟡10(저장소 정리)이 **코드로 이미 수정되어** 들어 있습니다. 검증도 끝났습니다(타입 검사 통과, 테스트 10/10 통과, 프로덕션 빌드 성공). 아래 순서대로만 진행하면 사이트에 반영됩니다.

⚠️ **시작 전 딱 하나만 기억하세요**: 이번 수정의 핵심은 "권한자 명단에 있는 계정의 승인·서명만 인정한다"입니다. 명단(2단계)을 등록하기 전까지는 **새로운 승인·서명이 전부 보류 처리**됩니다. 고장이 아니라 의도된 안전장치(fail-closed)이니, 1→2단계를 이어서 진행해 주세요. 기존에 이미 저장된 서명 상태는 그대로 유지됩니다.

---

## 1단계 — 코드 반영 (GitHub Desktop, 약 10분)

1. 압축파일(`awos-보안수정-2026-08-29.zip`)을 풉니다.
2. 풀린 폴더 안의 내용물 전체를 **컴퓨터의 awos 프로젝트 폴더에 덮어쓰기** 합니다. ("파일 교체" 물어보면 모두 교체)
3. GitHub Desktop을 열면 왼쪽에 변경된 파일 약 23개가 보입니다. (`.DS_Store`는 삭제로 표시되는 게 정상)
4. 아래쪽 Summary 칸에 `보안 수정: 서명 위조 방지, CSP, 프록시 제거 등` 이라고 적고 **Commit to main** → **Push origin**.
5. 푸시하면 Netlify가 자동으로 새 버전을 배포합니다(2~3분). https://aviationwalletos.netlify.app 접속해서 로그인이 되는지 확인하세요.

문제가 생기면: GitHub Desktop의 History에서 방금 커밋을 우클릭 → Revert 하면 이전 상태로 돌아갑니다.

## 2단계 — Supabase에 권한자 명단 만들기 (약 10분, 가장 중요!)

1. https://supabase.com/dashboard → 프로젝트 선택 → 왼쪽 메뉴 **SQL Editor**.
2. 압축파일 안의 `supabase/schema3-authorization.sql` 내용 전체를 붙여넣고 **Run**.
3. 이어서 기관 계정을 등록합니다. 새 쿼리 창에서:
   ```sql
   select id, email, created_at from auth.users order by created_at;
   ```
   를 실행해 **기관 계정으로 쓰는 이메일**의 `id` 값을 복사하세요.
4. 아래를 값만 바꿔 실행합니다 (기관이 여러 곳이면 반복):
   ```sql
   insert into public.authorized_orgs (user_id, org_name, note)
   values ('3번에서 복사한 id', '기관 이름', '최초 등록')
   on conflict (user_id) do update set org_name = excluded.org_name;
   ```
5. 확인: `select * from public.authorized_orgs;` 에 방금 등록한 기관이 보이면 성공.

이 순간부터 그 기관 계정의 [APPROVED] 댓글, 그리고 그 기관이 승인한 교관의 [SIGNED] 댓글만 유효하게 인정됩니다. 다른 누가 흉내 내도 무시됩니다.

## 3단계 — 배포 확인 후, 첨부파일 저장소 비공개 전환 (5분)

**반드시 1단계 배포가 끝난 뒤에** 진행하세요 (순서가 바뀌면 서명 이미지가 잠깐 안 보일 수 있습니다).

1. Supabase SQL Editor에서 `supabase/schema4-storage-private.sql` 내용 전체를 붙여넣고 **Run**.
2. 확인: `select id, public from storage.buckets where id = 'board-files';` 결과의 public이 `false`면 성공.
3. 사이트에서 서명 완료된 기록을 열어 서명 이미지가 잘 보이는지 확인하세요(이제 로그인한 사람에게만, 1시간짜리 임시 링크로 보입니다).

## 4단계 — 취약한 엑셀 라이브러리 교체 (내 컴퓨터에서 명령 1줄)

터미널(맥: 터미널 앱)에서 awos 프로젝트 폴더로 이동한 뒤:

```bash
npm install https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz
```

실행 후 GitHub Desktop에 `package.json` 변경이 뜨면 `xlsx 보안 버전으로 교체` 라고 적고 커밋·푸시하세요. 그다음 사이트에서 "엑셀 이관" 기능이 잘 되는지 한 번 테스트해 주세요.

## 5단계 — 옛날 aiapp 서버에 남은 데이터 확인 (10분)

1단계 배포로 앱은 더 이상 aiapp 서버를 전혀 쓰지 않습니다. 하지만 예전(1단계 시절)에 그 서버에 저장됐던 회원·비행기록이 **그쪽에 아직 남아있을 수 있습니다**.

1. aiapp(빌더 플랫폼) 콘솔에 로그인 → AWOS 프로젝트의 게시판/회원 데이터 확인.
2. 남아 있으면 직접 삭제하거나, 플랫폼 고객센터에 프로젝트 데이터 삭제를 요청하세요.
3. 확인이 끝나면 aiapp 쪽 프로젝트는 비활성화해도 됩니다.

## (선택) Netlify 환경변수 등록

이제 Supabase 주소·키가 `src/lib/supabase/env.ts` 한 곳에서 관리됩니다. 더 깔끔하게 하려면 Netlify → Site settings → Environment variables에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`를 등록할 수 있지만, **등록하지 않아도 기본값으로 정상 동작**하니 급하지 않습니다.

---

## 배포 후 확인 체크리스트

로그인 → 비행기록 1건 저장 → 새로고침해도 남아있는지. 교관 서명 흐름: 기관 미등록 상태에서는 서명이 인정되지 않는 것이 정상, 2단계 완료 후에는 승인된 교관의 서명만 인정. 서명 이미지가 상세 화면에 표시되는지(3단계 후). 엑셀 이관 정상 동작(4단계 후). 브라우저 개발자도구 콘솔에 빨간 CSP 오류가 없는지.

## 이번에 바뀐 파일 (23개)

새 파일: `src/lib/baas/authorization.ts`(권한 검증 핵심), `src/lib/supabase/env.ts`, `src/hooks/useSignedFileUrl.ts`, `supabase/schema3-authorization.sql`, `supabase/schema4-storage-private.sql`, `.gitignore` — 수정: 서명·승인 판정 관련 10개 파일, `netlify.toml`(프록시 제거+CSP), `public/sw.js`, `src/lib/supabase/client.ts`, `src/lib/baas/supabaseTransport.ts`, `package.json`(vitest) — 삭제: `.DS_Store`

## 아직 남은 큰 숙제 (다음 세션)

리포트의 🔴2 — 비행기록·자격증을 "로그인 회원 전원이 읽을 수 있는 게시판"에서 "본인만 읽는 정규 테이블"로 옮기는 이전 작업. 반나절~하루짜리 공사라 별도 세션에서 데이터 이전 스크립트와 함께 진행하는 것을 권합니다. 이때 3단계에서 넓게 열어둔 저장소 읽기 권한도 "관련 당사자만"으로 좁힙니다.
