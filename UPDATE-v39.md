# UPDATE v39 — 자격증 편집 폼 복원 버그 (2026-09-04)

## [BUGFIX] 수정 화면의 세부 종류가 항상 첫 항목(자가용)으로 열리던 문제
편집 모드에서 세부 종류 초기값을 저장된 자격에서 읽지 않고 **목록 첫 항목**으로 두고 있었습니다.
사업용을 열어도 "자가용 조종사(PPL)"이 선택돼 있었고, 그대로 저장하면 명칭이 자가용으로 바뀔 수 있었습니다.

- 저장된 명칭에서 세부 종류·보조 표기(형식, 무인 종 등)를 복원(`inferSubTypeFromName`) — 조종사·신체검사·EPTA·교육증명·경량·초경량 전부
- 복원이 되면 세부 종류를 바꿀 때 명칭도 함께 바뀌도록 자동 연동 켬. 복원이 안 되는 옛 형식 이름은 그대로 유지
- 종류/등급/형식/자격번호/제한사항/연결 자격증은 이미 구조화 필드에서 복원되고 있었음
- 회귀 테스트 추가(36건)

## 같이 확인한 것
- 한정 항목: v27 이후 등록분은 구조화 필드(종류·등급·형식)로 복원됨. 그 이전 이름만 있는 한정은 기본값으로 열림 → 다시 저장하면 새 형식으로 정리됨
