# UPDATE v24 — 출시 전 중간 점검 (2026-09-03)

검증: 타입 0 · 린트 0 · 테스트 32 · 빌드 통과.

## 이번에 고친 것 (코드로 확인된 문제)

### 1. [BUG] 같은 화면 안에서 설정값이 어긋나던 문제
`usePilotTracks`(자격 구분·생년월일·운항형태)와 `useVehicles`(기체 카드)가 LogbookPage·CurrencyDashboard·FlightReadinessPanel·AccountPage에서
**각자 따로 state를 들고 있었습니다.** 커런시 탭에서 운항형태를 바꾸면 그 컴포넌트만 바뀌고, 같은 페이지의 승무시간 카드·자격증 폼(1종 6개월 예외 판정)은
새로고침 전까지 옛 값을 썼습니다. 쓰기 후 이벤트를 쏘고 모든 인스턴스가 다시 읽도록 고쳤습니다. 다른 탭(storage 이벤트)도 동기화됩니다.

### 2. [성능] 초기 번들 941kB → 549kB (gzip 254 → 157kB)
LogbookPage·DashboardPage·AccountPage·InquiryPage를 라우트 단위로 분리(React.lazy). 랜딩·로그인 화면은 로그북 코드를 내려받지 않습니다.
모바일 첫 진입이 눈에 띄게 빨라집니다. 로그북 진입 시 "불러오는 중…" 짧게 표시.

### 3. [모바일] iOS에서 인쇄가 안 되는 문제 선제 대응
iOS Safari는 숨은 iframe의 print()를 무시하는 경우가 있습니다. iOS로 감지되면 새 탭에 문서를 열고(공유 → PDF 저장 경로), 그 외는 iframe 인쇄.

### 4. [모바일] 탭바가 3줄로 늘어나 화면을 잡아먹던 문제
sticky 탭바가 모바일에서 줄바꿈되면 상단 고정 영역이 커집니다. 모바일은 가로 스크롤, sm 이상은 기존처럼 줄바꿈.

### 5. [모바일] 토스트가 iOS 홈 인디케이터에 가려지던 문제
`env(safe-area-inset-bottom)` 반영.

### 6. [PWA] 서비스워커 캐시 정리
정적 자산 캐시가 배포마다 쌓이기만 했습니다(해시 파일명이라 무한 증가). 120개 초과 시 오래된 것부터 삭제. 캐시 버전 v2로 올려 기존 캐시 폐기.

---

## 실제 기기에서 확인해야 하는 것 (코드로는 못 잡음)

| 항목 | 확인 방법 | 예상 위험 |
|---|---|---|
| **iOS 홈화면 설치(PWA) 상태에서 인쇄** | 설치한 앱에서 "비행경력증명서 PDF" | standalone 모드는 인쇄 자체가 없을 수 있음. 안 되면 "Safari에서 열기" 안내 필요 |
| **iOS 소셜 로그인 리다이렉트** | 설치한 앱에서 카카오/구글 로그인 | standalone에서 외부 브라우저로 튕겼다가 돌아올 때 세션 유실 가능. 이 경우 로그인은 Safari에서 하도록 안내 |
| **날짜 input 모바일 키보드** | 안드로이드 크롬·iOS에서 발급일 입력 | 달력 UI는 다크로 바뀌었으나, 기기별 표시가 다름 |
| **숫자 입력 키보드** | 비행시간·아워미터 칸 탭 | `type=number`라 소수점 키보드가 뜨는지 기기별 확인. 안 뜨면 `inputMode="decimal"` 추가 |
| **긴 이름·긴 기종명** | 이름 10자 이상, 기종명 20자 이상 계정 | 히어로·덱 카드 줄바꿈. 대부분 truncate 처리돼 있으나 눈으로 확인 |
| **엑셀 가져오기(모바일)** | 폰에서 xlsx 선택 | 파일 선택기 동작, 429kB xlsx 청크 로드 시간 |
| **저장공간** | 기록 수백 건 + 사진 첨부 | localStorage 5MB 상한. 서명 이미지(dataURL)가 기록에 포함되면 빨리 참. 사진은 이미 서버 버킷이지만 서명 dataURL은 확인 필요 |
| **오프라인 재열람** | 비행기 모드에서 앱 열기 | SW 셸 폴백 동작. 로그북 청크가 캐시돼 있어야 함(한 번 열었으면 됨) |

## 코드에서 보완이 필요한 것 (다음 작업 후보, 우선순위순)

1. **프로필 설정 중복 fetch** — `useProfileSettingsSync`가 인스턴스마다 게시글 목록을 조회. 지금은 4번 호출. 동작엔 문제없지만 진입 시 요청 4개가 겹침. Context로 올려 1회로 줄일 것.
2. **`window.confirm` 3곳** — 전체 삭제·기체 삭제·자동분류 확정. 모바일에서 OS 기본 창이라 촌스럽고, PWA에선 제목에 도메인이 붙음. 앱 내 확인 다이얼로그로 교체.
3. **응시경력 진척도 UI 미배선** — `lib/eligibility/rules.ts` 엔진은 있으나 화면 없음. 필드(단독·감독하·구간거리)도 폼에 안 보임. v1.2.
4. **엑셀 이관 시 자격 구분 추정** — 현재는 이관 시점 구분으로 일괄 태그. 섞인 파일 대비해 기종 열로 추정 보강.
5. **`any` 없음, console.log 없음** — 양호. 다만 `data-mbaas-oid` 속성이 전 컴포넌트에 남아 있음(빌더 흔적). 기능엔 무해하나 DOM이 무거워짐. 출시 전 일괄 제거 고려.
6. **테스트 범위** — 시간 계산·규칙 엔진·트랙 분류는 있으나 **동기화 훅·폼 검증은 없음**. 회귀가 잦은 곳이라 다음에 붙일 것.
7. **에러 바운더리 없음** — 한 컴포넌트가 던지면 화면 전체가 하얘짐. 라우트 단위 ErrorBoundary + "다시 시도" 버튼.

## 디자인에서 눈에 띈 것

- 히어로의 "실시간 비행 적합성" 패널과 덱 카드가 모바일에서 세로로 길게 쌓임 — 정보량은 맞지만 첫 화면이 스크롤 3번. 덱을 접기(1장만 보이고 탭하면 펼침) 고려.
- 커런시 탭의 운항형태 토글이 카드 제목보다 시각적으로 강함 — 크기 한 단계 낮춰도 됨.
- 초경량 폼의 ①~⑦ 번호는 로그기록지와 맞추려는 의도지만, 앱 사용자에겐 낯설 수 있음. 실사용자 반응 보고 결정.
