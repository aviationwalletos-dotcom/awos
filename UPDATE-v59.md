# UPDATE v2.9i — schema24 실증 · 깨진 클래스 3개 추가 (2026-09-13)

시간이 더 있다고 하셔서, **"확인 못 했다"고 넘긴 것들을 실제로 확인**했어요.
v2.9g·v2.9h 를 포함해요. 이것만 올리시면 돼요.

---

## 1. schema24 를 진짜 Postgres 에서 돌려봤어요 ✅

컨테이너에 PostgreSQL 16 을 설치하고 Supabase 를 흉내 낸 환경을 만들었어요
(`auth.users`, `auth.uid()`, `authenticated` 롤, schema2 의 쓰기 정책).
그 위에 **제가 드린 `schema24-board-privacy.sql` 을 그대로** 적용했어요.

사용자 4명(학생A·학생B·교관C·관리자D), 게시판 6종에 글을 하나씩 깔고 조회 결과예요.

### 게시글

| 게시판 | 학생A(본인) | 학생B(남) | 교관C | 관리자D |
|---|---|---|---|---|
| 비행기록 | 보임 | **안 보임** | **안 보임** | 보임 |
| 자격증 | 보임 | **안 보임** | **안 보임** | 보임 |
| 문의 | 보임 | **안 보임** | **안 보임** | 보임 |
| 업무기록 | 보임 | **안 보임** | **안 보임** | 보임 |
| 개인설정 | 보임 | **안 보임** | **안 보임** | 보임 |
| 상태공유 | 보임 | 보임 | 보임 | 보임 |

### 댓글 (문의 답변)
관리자D 가 학생A 의 문의에 남긴 `"연락처는 010-…"` 답변이

- 학생A(작성자) → **보임** ✅
- 관리자D → **보임** ✅
- 학생B(남) → **안 보임** ✅
- 교관C → **안 보임** ✅

### 앱이 망가지지 않는지도 확인

| 동작 | 결과 |
|---|---|
| 관리자D 가 답변 댓글 달고 즉시 되읽기(PostgREST `return=representation`) | 성공 |
| 학생A 가 본인 비행기록 수정(동기화 흐름) | 성공 |
| 학생A 가 본인 기록 목록 조회 | 성공 |
| 학생B 가 남의 비행기록 수정 시도 | **0건** |
| 학생B 가 `post_id` 로 남의 문의 답변 직접 조회 | **0건** |
| 학생B 가 남의 업무기록 전수 조회 | **0건** |

### 성능 (탈론 798건을 감안해 2,000건으로)

| 질의 | 시간 |
|---|---|
| 게시글 2,000건 | 0.97 ms |
| 댓글 2,000건 (정책에 하위질의 있음) | 2.42 ms |

Postgres 가 `authorized_orgs` 확인을 **InitPlan 으로 한 번만** 계산하고, 댓글의 부모 글
확인도 **hashed SubPlan** 으로 묶어요. 행마다 반복하지 않아요.

> 실환경 확인은 여전히 해 주세요. 제 harness 는 실제 Supabase 가 아니고,
> 운영 DB 의 정책이 손으로 바뀌어 있을 수도 있어요.

---

## 2. 깨진 Tailwind 클래스 3개 추가 발견

v2.9g 에서 "이중 슬래시"만 고쳤는데, 그건 **증상**이었지 원인이 아니었어요.
진짜 문제는 **"코드처럼 보이지만 타입 검사를 안 받는 문자열"** 이에요.

그래서 방식을 바꿨어요. 소스의 `className` 에서 Tailwind 후보 **516개**를 뽑고,
실제 생성된 CSS 와 **하나씩 대조**했어요. 3개가 더 나왔어요.

| 클래스 | 어디 | 무슨 일이 일어나나 | 고침 |
|---|---|---|---|
| `bg-base` ×2 | `LegacyExcelImport.tsx` 663·677 | 엑셀 "읽기 방식" 박스의 **날짜순서·시간단위 드롭다운에 배경색이 없음.** 글자가 `text-ink`(거의 흰색)라 브라우저 기본 밝은 배경 위에서 잘 안 보임 | `bg-panel` |
| `h-4.5 w-4.5` | `TsIntegrationCard.tsx` 25 | Tailwind 기본 간격에 **4.5 가 없음.** 아이콘 크기가 안 먹어서 lucide 기본값 24px 로 커짐 | `h-[18px] w-[18px]` |
| `h-4.5 w-4.5` | `sections/Problem.tsx` 34 | 〃 | 〃 |

`bg-base` 는 추측이 아니에요. **같은 파일 716행의 `<select>`** 가 똑같은 클래스 조합
(`rounded-control border border-white/10 bg-panel px-3 py-1.5 text-sm text-ink`)에
`bg-panel` 을 쓰고 있어요. 대조로 답이 나왔어요.

`4.5` 는 18px 이라, 관례인 `h-4`(16px) 대신 **의도한 18px 을 그대로** 살렸어요.
집 관례에 맞추고 싶으시면 `h-4 w-4` 로 바꿔도 돼요.

### 오탐도 있었어요
`bg-blueprint`·`font-mono-data`·`table-scroll` 은 `src/index.css` 에 직접 정의돼 있어서
정상이에요. `bg-[radial-gradient(…)]`·`py-[clamp(…)]` 같은 대괄호 값도 전부 잘 생성돼요.

---

## 3. 같은 방식으로 훑었는데 **깨끗한** 것들

"조용히 틀릴 수 있는 문자열"을 종류별로 전수 대조했어요. 아래는 전부 이상 없어요.

| 검사 | 결과 |
|---|---|
| 코드가 호출하는 RPC 8개 ↔ SQL 정의 | 전부 일치 ✅ |
| 코드가 쓰는 테이블 4개 ↔ SQL 정의 | 전부 일치 ✅ |
| `schema24` 의 board id ↔ `config.ts` | 일치, 빠진 둘은 프로젝트 ID·상태공유 ✅ |
| `COMMIT_REF` (Netlify 배포 식별) | 이름 맞음 ✅ |
| 날짜 파싱 시차 처리 | `T00:00:00`(Z 없음) → 로컬 기준 일관 ✅ |

### 버려진 테이블 7개 — 안전했어요
코드가 안 쓰는 테이블이 12개 있어요(`certificates`, `logbook_entries`, `work_logs`,
`fec_requests`, `instructor_approvals`, `signature_requests`, `status_shares` 등).
예전 설계의 잔해인데, **거기 실데이터가 남아 있고 RLS 가 없으면** 지금까지 고친 게
무의미해져요. 확인했어요.

전부 RLS 가 켜져 있고, 정책도 `auth.uid() = user_id` 또는 기관 소속 기준이에요.
`using (true)` 인 게 하나도 없어요. ✅

---

## 4. localStorage 용량 — 계산해 봤어요

`SignaturePad.tsx` 주석이 "수백 건에서 5MB 상한에 닿는다"고 걱정하고 있어서 실측했어요.

| 기록 형태 | 건당 | 798건 사용량 | 5MB 한계 |
|---|---|---|---|
| 기본(서명 없음) | 579 B | 0.44 MB (9%) | 약 9,000건 |
| + 교관 전자서명 | 766 B | 0.58 MB (12%) | 약 6,800건 |
| **+ 본인 서명 이미지** | **4.4 KB** | **3.32 MB (66%)** | **약 1,200건** |

**위험은 본인 서명 이미지예요.** 그게 없으면 여유가 많아요.
5MB 는 자격증·기체·tombstone 까지 **다 합쳐서** 쓰는 값이라 실제 한계는 더 낮아요.

### 그런데 급하지 않아요 — 설계가 이미 막고 있어요
- 로컬 저장 실패(`QuotaExceededError`)는 **조용히 죽지 않아요.** 경고 + 토스트가 떠요
- 서버 동기화는 **별도로** 돌아서 데이터가 안 없어져요
- 로그인할 때 서버에서 **병합**해 와요
- 삭제는 3중(로컬 tombstone + 서버 content 마커 + 서버 삭제)이라, 로컬이 꽉 차서
  tombstone 이 안 써져도 **지운 기록이 되살아나지 않아요**

당장 할 일은 없어요. 1,000건 넘는 사용자가 생기면 그때 서명 이미지를 서버 스토리지로
옮기는 걸 검토하면 돼요.

---

## 5. 별표 18 제1호 — 알아두실 것 (조치 안 함)

원문은 **"연속 24시간 동안"** 이에요. 앱은 **"달력상 하루"** 로 계산해요.

로그북에 시각이 없어서(날짜만) 굴러가는 24시간 창을 만들 수가 없어요. 어쩔 수 없는
근사인데, **방향이 관대한 쪽**이에요. 예를 들어 13일 저녁 5h + 14일 아침 5h 는
실제로는 14시간 안에 10시간을 탄 건데, 달력 기준으로는 각각 5h 라 GO 로 보여요.

승무시간 카드가 자가용에는 "참고"로 뜨고, 별표 18 자체가 운항승무원 기준이라
지금 구조에서 문제가 되진 않아요. 다만 **알고 있는 근사**로 기록해 둬요.

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    109 통과
npm run build                     통과
npx playwright test --list        20건
Tailwind 클래스 516개 전수 대조    깨진 것 0개
PostgreSQL 16 RLS 시나리오        13개 전부 의도대로
```

---

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. Supabase → `schema24-board-privacy.sql` Run (아직이면) → 확인 쿼리 3줄 `true`
3. 배포 뒤 확인
   - NO-GO 배지 **빨간 바탕**
   - **문의 답변**이 작성자 계정에서 보이는지
   - **AI 읽기** 정상 동작
   - **엑셀 가져오기의 "읽기 방식" 드롭다운** 글자가 잘 보이는지 ← 이번 수정
4. 별표 18 제4호 적용 대상(사업자 한정인지) 확인
