# AWOS 인수인계 (2026-09-10 밤) — 새 채팅 첫 메시지에 붙이기

한국어 해요체. 결론 먼저, 지시는 하나씩. zip 누적 배포. 이 문서 + `UPDATE-v56.md`(상세 이력)로 이어서 작업한다.
**채팅은 한 번에 하나만.** 새 창을 열면 이전 창은 닫는다(9/10에 두 창이 같은 폴더를 써서 꼬일 뻔했다).

## 0. 지금 상태 한 줄
**v2.0 배포 직전.** 실사용자 테스트(5명) 진행 중. 법령 관리(조문 단위 감지·시행 예정·매뉴얼)까지 완성. TS 미팅 9/14~22, 문서 발송 대기.

## 1. 기본
- React 19 + Vite + TS + Supabase + Netlify(main 자동 배포). 저장소 github.com/aviationwalletos-dotcom/awos · Supabase vflyqnbdquaanpkvuinz
- 관리자 aviationwalletos@gmail.com · 문의 awos.help@gmail.com · 사이트 aviationwallet.com
- 배포: Claude 가 `/home/claude/awos` 수정 → `awos-<버전>.zip`(영문명) → 대표가 덮어쓰기 → GitHub Desktop Push. **zip 은 삭제를 못 전하므로 지운 파일은 `DELETE-LIST-<날짜>.md` 로.** 파일명은 영문만(한글 파일명이 Netlify 빌드를 깨뜨림, 9/10).
- 검증: `npx tsc --noEmit` → `eslint src --max-warnings=0` → `vitest run`(99) → `npm run build` → `playwright test --list`(20). 컨테이너엔 브라우저 없음.
- 실행된 SQL: schema11~19 완료. **schema20(교관 확인 endorsement)·schema21(첨부 여러 장) 실행 필요.**
- Netlify 환경변수: `ANTHROPIC_API_KEY`(월 한도 $30 설정 권장), `SUPABASE_URL`, `SUPABASE_ANON_KEY`(sb_publishable_…), `LAW_GO_KR_OC`(=aviationwallet, 법령정보센터 OPEN API 승인·본문·별표 JSON 체크 완료). 선택: `VITE_SENTRY_DSN`(넣으면 오류 모니터링 켜짐).

## 2. 9/10 하루에 끝난 것 (v1.4h → v2.0)
- **엑셀 가져오기**: 1:30·90분·분 단위 열 자동 감지, 미국식 날짜·두 자리 연도·엑셀 일련번호, "읽기 방식" 박스.
- **헤더**: 로고가 홈 버튼(로그북·대시보드·계정). 관리자에게 "로그북으로" 뜨던 버그 수정. 대시보드에 계정·로그아웃.
- **자격증 한 장 → 여러 카드**: CPL 사진에서 한정·계기·초급/선임 교관·항공영어 체크 목록. EPTA 만료 자동(4급 3년·5급 6년·6급 영구). 제목 "자격증 · 한정 등록". 중복 등록 차단.
- **AI 읽기**: 국외 이전 동의 체크(기기당 1회), 하루 10회/계정(schema18), 안내문에서 변수명 제거.
- **주간 시간 버튼**: 증명서 폼에서 총−야간=주간 계산 제안(AI 는 계산 안 함).
- **탈퇴 정책(schema17)**: "내 기록에 적힌 상대방 이름은 남는다". 학생 탈퇴해도 교관 서명함의 이름·날짜·스냅샷·해시 유지, 이메일만 삭제. 탈퇴 전 CSV 백업 단계. 유예 없이 즉시. "탈퇴한 사용자" 배지.
- **내 데이터 전체 내보내기(JSON)**, 최근 로그인 표시, TS 문구 제거(랜딩·앱 내 카드).
- **법령 원문 대조 완료**(대표가 보낸 2026-07-01 PDF + 별표 4·8·18): 본문 조문 전부 일치. **별표 18 "7일 35h"는 근거 없어 삭제, 30일→28일 정정.** `appliedEffectiveDate` 박음.
- **법적 빈틈 11개 반영**: 한정 없는 등급은 GO 대신 "한정 없음"(제37조), 8.2.4 회복 안내, 야간 야외 칸(별지 36호), FTD·BATD 착륙 제외, 자가용 승무시간은 "참고", 만료일 안내(제99조③·제92조③), IR 1종 안내, PPL 17세, 단독 야외 매 비행 확인.
- **법령 관리 2단계**: 목록 API(law·admrul) + **본문 API(lawService, Referer 로 통과)** 로 조문 단위 해시 비교, 별표는 별표목록 API(licbyl, 6자리 번호) + PDF 파일 해시, 시행 예정(eflaw) 노란 띠. `regulation_checks.article_hashes`(schema19). **"확인함"으로 기준 저장 완료(9/10).**
- **개정 대응 매뉴얼** `docs/regulation-update-manual.md` + 탭의 "매뉴얼 복사" 버튼. 새 Claude 에 붙여 넣으면 법령 값 수정 가능.
- **개인정보처리방침** `public/privacy.html` 갱신: 국외 이전 표(Anthropic·Sentry), 탈퇴 후 서명 기록 보존(정당한 이익), AI 사용량 30일, 보호책임자.
- **Sentry**(`src/lib/monitoring.ts`): DSN 있을 때만 켜짐, PII 안 보냄. **탈퇴 E2E**(`e2e/06-withdrawal.spec.ts`, 버리는 계정 자동 생성, Auto Confirm 필요).

- **교관 확인(Endorsement) = 교관 전자서명**(v2.2, schema20): 자격증 탭 "교관 확인" 구분에서 승인 교관을 고르면 `approval_requests kind='endorsement'`(스냅샷·해시)가 교관 서명함으로 감 → 손글씨 서명 → 카드 "교관 서명됨". 관리자 큐엔 안 뜸. 탈퇴해도 남음(서명 기록).
- 기록 입력 "야간 크로스컨트리" 자동 채움(min(야간, XC)), 상세·CSV·엑셀 가져오기에 야간 XC 반영.
- **첨부 여러 장**(v2.3, schema21): 자격증(앞·뒷면)·비행경력증명서(여러 쪽) 최대 5장. `approval_requests.attachment_paths text[]`(첫 장은 `attachment_path` 유지). 관리자 "첨부 보기 (N장)". 첫 장을 AI 가 읽음.

## 3. 법령 사실 (9/10 원문 확인)
- **제76조② 는 2026-11-13 삭제 → 제39조의4 신설**(소지 의무 이동). 같은 날 제44조②3호 신설. 그날 화면·문서의 "제76조②"를 바꿀 것(법령 관리 탭 노란 띠로 뜸).
- 제77조②나: 기장 본인 경력은 조종교육증명+제125조 경험 있는 교관이 증명 → 관리자가 교관 승인 때 조종교육증명을 보는 근거.
- 8.1.7.6 나 단서: 항공사업 조종사는 사업자 전산 출력물로 로그북 대체 가능 → Q1 논거.
- 조종사 개인 로그북 보존 기간 조문 없음(제56조③ 15개월은 사업자 의무).
- 8.2.3 은 접근 6회 **+ 계기비행 6시간**. 8.2.4 마(2인조종 SIC 12개월 3회)는 미구현·범위 밖.

## 4. TS 문서
- `AWOS-기능소개-v2.pptx`(14장: K-EPL 생성원 슬라이드·서명 해시·AI 읽기 반영), `AWOS-질의-v4.pptx`(6장: Q1 8.1.7.6 단서, Q2 제77조①·②다 고시 여부, Q3 SHA-256, Q5 K-EPL 생성원, Q6 제76조②→제39조의4, Q7 해외 기록).
- **미결**: Q8(탈퇴 서명 기록 K-EPL 보존) 추가 여부, 소개서 8번에 제77조② 근거 한 줄, **발송 시점**(9/14 미팅이면 9/11이 마지막).
- 미팅 준비: Supabase Pro 결제(백업), 창업보육센터 입주·사업자 등록 예정 언급.

## 5. 다음 과제 (우선순위)
1. TS 문서 발송 + 미팅.
2. 해외(미국) 훈련 기록 구분 설계 — 대표 결정 대기(서명 요청 안 뜨게, 착륙 기재 없음).
3. 저장소 정리: `DELETE-LIST-0910.md`. 루트의 UPDATE-v10~v55 등 옛 md 를 `docs/history/`로 옮길지 결정.
4. 외부 공개 전: Supabase Pro, Sentry DSN, 네이버 서치어드바이저 코드, 오프라인 실기 확인(폰 비행기 모드), 이메일 알림(법령 변경·서명 요청).
5. 보류: 탈론 연동, 자격증 자동 인증(TS API), 경량 커런시, DJI CSV, Remote ID.

## 6. 결정·정책 (바뀌지 않는 것)
- 이월 기록 승인 주체는 관리자(대표). "기관·학교" 표현 금지. 나중에 기관과 협의되면 기관이 그 역할.
- AI 는 문서에 없는 값을 만들지 않음(계산은 앱, 판단은 사람). 법령 값 자동 수정 없음(감지까지만).
- 탈퇴: 상대방 기록 속 이름은 남음. 즉시 삭제, 유예 없음(테스트 단계).
- 카드덱 유지, 기록 폼 접지 않음, 승무시간 카드는 모두에게(자가용은 "참고").
- GO 배지: 하나라도 가능이면 GO, 단 한정 없는 등급은 제외.
- 문의 awos.help@gmail.com. 해요체. 화면에 버전 번호·지명 없음.

## 7. 알아두기
- `account.id` = auth uuid, `account.user_id` = 이메일. `approval_requests` 가 서명·승인 단일 테이블(`requester_id` 는 set null). `dayLandings: undefined = 기재 없음`. `conditions.nightCrossCountry` 는 9/10 이후 기록만.
- 관리자 = `authorized_orgs`(`is_awos_admin()`). 자기 서명은 서버에서 차단.
- 법령정보센터: 목록·본문·별표 API 모두 **Referer: https://aviationwallet.com/** 헤더가 있어야 통과. 주소창 직접 호출은 실패가 정상. 행정규칙 한글주소는 띄어쓰기 그대로.
- 알고 받아들인 위험: 기록 자기 신고, 두 기기 동시 수정 후승, 1종 6개월 예외를 "상업 40세↑" 전부에 보수 적용, API 키 스크린샷 노출(월 한도로 방어).
- 대표(우혁준)는 초보 개발자·조종사. 설명 짧게, 법 근거는 조문 번호로, "딱 하나만".
