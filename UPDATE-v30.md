# UPDATE v30 — 모바일 정리 8건 (2026-09-03)

검증: 타입 0 · 린트 0 · 테스트 32 · 빌드 통과.

1. **총 비행시간 요약 접기** — 총 블록타임만 보이고 "상세 내역"(범주·임무·조건별)은 접힘 기본. `components/Collapsible.tsx`
2. **주간 시간 자동 기입** — 블록타임을 넣으면 주간 = 블록타임, 야간 = 0 자동. 야간 칸을 고치면 주간이 자동 보정. 직접 고친 값은 유지.
3. **자격증 목록 간소화** — 카드 그리드 → 한 줄 행(색 띠 · 명칭 · 상태 배지 · 구분/발급기관/만료 · D-day). 폰에서 스크롤 1/3.
4. **커런시 섹션 접기** — 최근 비행경험 / 계기 / 교관 각각 접힘 기본. 접힌 상태에서도 헤더에 충족·미달 배지 표시.
5. **[BUGFIX] 서명 "Failed to fetch"** — 서명 이미지를 Blob으로 바꿀 때 `fetch(data:...)`를 썼는데 CSP connect-src에 `data:`가 없어 브라우저가 막았습니다. 네트워크 없이 base64를 직접 디코드하도록 수정.
6. **관리자 대시보드 탭 고정 위치** — 헤더 높이 실측(로그북과 동일). 모바일은 가로 스크롤.
7. **schema8 실행 결과** — 아래 답변 참고(오류 아님).
8. **로그인 상태의 "시작하기"** — 랜딩 히어로·하단 CTA가 로그인돼 있으면 "내 로그북 열기"로 바뀌고 /logbook 으로 이동.
