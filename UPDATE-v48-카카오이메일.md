# UPDATE v48 — 카카오 이메일 계정 반영 (2026-09-04)

## 증상
비즈 앱 전환·이메일 동의항목 설정 후 카카오 로그인 동의 화면에는 이메일이 뜨지만, **전환 전에 가입한 카카오 계정**은 계정정보의 이메일이 비어 있음.
Supabase는 최초 가입 시 받은 이메일만 `auth.users.email`에 넣고, 이후 로그인에서 새로 받은 이메일은 `identities[].identity_data.email`에만 들어옴.

## 수정
1. 서버 컨텍스트의 계정 이메일(`이메일(아이디)` 표시·소속·서명 요청 식별 등)이 비어 있으면 **소셜 identity 이메일로 대체** (`emailOf()`).
2. 계정정보에 **"(카카오 제공)" 표시 + "○○@… 를 계정 이메일로 등록" 버튼**. 누르면 Supabase가 그 주소로 확인 메일을 보내고, 링크를 누르면 `auth.users.email`이 확정됨 → 이메일 로그인 연결·비밀번호 재설정·안내 메일 가능.
3. 새로 가입하는 카카오 사용자는 처음부터 이메일이 들어오므로 해당 없음.

## 배포 후
재헌님·우혁준님처럼 이미 카카오로 가입한 계정: 계정정보 → 이메일 옆 노란 안내의 **등록 버튼** → 메일 확인 링크 클릭 → 끝.
