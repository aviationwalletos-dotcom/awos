# UPDATE v2.9n — 주간 자동 계산 · 서명 표 가독성 (2026-09-13)

v2.9m 까지 포함해요. **SQL 없어요.**

---

## 1. 주간 시간 자동 계산

### 왜 바꿨나
예전에는 `총 − 야간 = 주간 넣기` 버튼을 눌러야 했어요. 9/10 에 "계산은 앱이 하고 넣을지는
사용자가 정한다"는 뜻으로 만든 건데, **실제로는 선택지가 아니었어요.**

- 주간이 비면 **저장이 막혀요**(검증). 버튼을 누르거나 같은 숫자를 손으로 치는 수밖에 없어요
- 버튼을 눌러도 저장되는 건 숫자뿐이에요. `day: 73.9` 만 남고 **"계산된 값"이라는 흔적이
  어디에도 안 남아요**. 관리자가 구분할 방법이 없었어요

즉 **강제인데, 흔적은 안 남고, 클릭만 하나 더** 있었어요. 셋 다 목적을 못 이뤘어요.

### 근거
**주간 + 야간 = 총 비행시간 은 항상 성립해요. 모의계기·FTD 는 총 비행시간에 들어가지
않아요** (2026-09-13 대표 확인).

"AI 는 문서에 없는 값을 만들지 않는다"는 정책은 **그대로예요** — AI 는 여전히 주간을
비워 두고 "직접 채워 주세요"라고 해요. 이 뺄셈은 AI 가 아니라 앱이 해요
("계산은 앱, 판단은 사람").

### 바뀐 동작

| | 전 | 후 |
|---|---|---|
| 총·야간 입력 시 | 버튼이 뜸 | **바로 채워짐** |
| 클릭 | 필요 | 불필요 |
| 안내 | 빨간 오류 "버튼을 누르거나…" | 회색 한 줄 "총 77.6 − 야간 3.7 로 계산해 넣었어요" |
| 저장 차단 | 있음 | 없음 |
| 수정 | 가능 | 가능 (고치면 안내가 사라져요) |

증명서에 주간이 직접 적혀 있으면 **그 값이 우선해요.** 이미 값이 있으면 자동 계산이
동작하지 않아요.

비고에는 따로 적지 않아요(2026-09-13 대표 판단).

---

## 2. 서명 표의 `{}`·`""` 제거

`서명 대상 기록` 표가 중첩 값을 날것의 JSON 으로 보여주고 있었어요.

```
전:  비행 조건    {"day":0.8,"night":0}
     조종 시간    {"dualReceived":0.8}
     범주별 시간   {}

후:  비행 조건    주간 0.8 · 야간 0
     조종 시간    Dual 0.8
     (범주별 시간은 담긴 값이 없어 아예 안 보여요)
```

### 어디가 바뀌었나
- `components/account/InstructorSignatureInboxSection.tsx` — 교관이 서명할 때 보는 표
- `components/logbook/EntryDetailDialog.tsx` — 서명 후 수정된 항목 대조표(전/후)

둘 다 `lib/approvals/snapshot.ts` 의 새 함수를 같이 써요.

```ts
SIGNED_SUBFIELD_LABEL   // 중첩 키의 한글 이름 (day → 주간, dualReceived → Dual …)
formatSnapshotValue()   // 값 한 칸을 읽히게
isEmptySnapshotValue()  // 담긴 값이 없는 칸을 표에서 뺌
```

### 값은 바꾸지 않아요
**0 은 0 으로 그대로 보여줘요.** 로그북에서 `0` 과 `기재 없음`(undefined)은 다른 뜻이고,
이 표는 "이 내용에 서명한다"는 증거라 임의로 숨기면 안 돼요. 단위 테스트로 고정했어요.

빈 객체 `{}` 만 표에서 빼요. 보여줄 값이 아예 없다는 뜻이에요.

모르는 키는 **키 이름 그대로** 남겨요. 나중에 필드가 늘어도 조용히 사라지지 않아요.

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    121 통과 (114 + 신규 7)
npm run build                     통과
npx playwright test --list        20건
```

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. 배포 뒤 확인
   - 비행경력증명서 입력에서 **총·야간만 넣으면 주간이 저절로 채워지는지**
   - 주간을 손으로 고치면 **안내 문구가 사라지는지**
   - 교관 서명함의 **서명 대상 기록 표에 `{}` 가 안 보이는지**

삭제된 파일 없어요.
