# UPDATE v15 — v1.1 자격·트랙 구조 (2026-09-03)

"v1.1 구현 시작" 신호에 따른 첫 구현분입니다. 실사용자가 없는 상태에서 구조를 바꾸는 것이라
마이그레이션 부담이 가장 낮은 시점에 넣었습니다.

## 검증

| 항목 | 결과 |
|---|---|
| 타입 검사 | 통과 |
| 린트 | 0건 |
| 테스트 | 19건 → **26건** 전부 통과 (버그 1 회귀 테스트 포함) |
| 프로덕션 빌드 | 통과 |

---

## 1. [BUGFIX] 항공기 시간이 드론 누적시간으로 잡히던 문제 (가장 치명적)

**원인.** 로그는 계정당 한 덩어리였고 역할(`individual_role`)은 화면 모드만 바꿨습니다.
`computeDroneCompliance(entries)`가 전체 로그를 합산하니 C172·DA42로 쌓은 시간이 그대로
"지도조종자 100시간" 진척도에 들어갔습니다.

**해법.** 비행 1건마다 트랙 태그(`vehicleClass`)를 달고, 모든 집계는 현재 트랙의 기록만 씁니다.

- `src/lib/tracks.ts` (신규) — 트랙 모델. `filterEntriesByTrack()`, `entryTrack()`, `inferVehicleClass()`
- `LogbookPage` — `entries` 대신 `trackEntries`로 총계·커런시·컴플라이언스·목록·내보내기 계산
- `roleCompliance.ts` — `computeUltralightCompliance()`로 교체. 유인(200h)·무인(1종 100h)을 종류별로 따로 계산
- `src/lib/tracks.test.ts` — **회귀 테스트**: C172 1.5h + DA42 2.0h + 드론 0.5h → 초경량 진척도는 0.5h여야 함

**기존 기록 처리.** 태그가 없는 기록은 기종명으로 추정합니다(훈련기·FTD는 항공기, "드론/멀티콥터/DJI"는 초경량).
추정된 기록 수를 히어로에 노란 배너로 알려주고, 기록을 열어 트랙을 확인하면 배너가 줄어듭니다.
기본값이 항공기인 이유는 이 앱의 기존 사용자가 전원 울진 훈련기 조종사이기 때문입니다.

## 2. 역할 → 보유 트랙(복수 선택)

역할 3종 확정안을 **하나만 고르는 역할이 아니라, 여러 개 가질 수 있는 트랙**으로 구현했습니다.

- 계정정보 → "보유 트랙 설정": 항공기 조종사 / 경량항공기 조종사 / 초경량비행장치 조종자 **체크박스**
- 로그북 히어로에 트랙 전환 탭(트랙별 기록 건수 배지). 트랙마다 덱·집계·커런시가 독립
- 기존 `individual_role`은 유지(옛 화면·기관 대시보드 호환). 트랙 저장 시 첫 트랙에 맞춰 자동 갱신
- 저장 구조는 기존 3중(localStorage → "개인설정" 게시글 JSON → profiles best-effort). **SQL 없이 동작**

`src/hooks/usePilotTracks.ts` (신규), `profileSettingsSync.ts`, `baas/types.ts`, `AccountPage.tsx`

## 3. 트랙별 자격 카드 덱 (7 / 4 / 3)

`src/data/deckDefs.ts` (신규) — 확정안 그대로.

| 트랙 | 카드 |
|---|---|
| 조종사 (7) | 자격증명 · 한정(종류·등급 수상 포함·형식) · **계기비행증명(분리)** · 조종교육증명(초급·선임×종류) · 항공신체검사 · **항공영어 EPTA** · 무선통신사 |
| 경량 (4) | 자격+한정 5종 · 교육증명 · 2종 또는 운전면허 · 무선 |
| 초경량 (3) | 조종자증명(유인 + 무인 5종류×1~4종) · 지도조종자/실기평가조종자 · 교육이수 |

카테고리 15종으로 확장(`types/certificate.ts`). 신체검사·무선통신사는 트랙 공유 카테고리로 처리해
경량 덱에서도 항공기 트랙에 등록한 신체검사가 보입니다.

## 4. 자격 등록 폼 일반화

`CertificateForm.tsx`를 "구분 → 세부 종류" 표 기반으로 다시 짰습니다. 새 자격을 추가할 때는
`certificateOptions.ts`의 표와 `deckDefs.ts` 두 곳만 손대면 됩니다.

- 한정: 종류(비행기/헬리콥터) · 등급(SEL/MEL/SES/MES) · 형식. **계기한정은 계기비행증명으로 이동**
- 조종교육증명: 초급·선임 × 비행기·헬리콥터
- EPTA: 4등급 3년 · 5등급 6년 · 6등급 영구 — 만료일 자동 계산
- 경량: 종류 5종, 운전면허(2종/1종)
- 초경량: 유인 7종 + 무인 5종류(수직이착륙기 포함, 1~4종 표기), 지도조종자·실기평가조종자, 교육이수 4종

## 5. 항공신체검사 유효기간 — 별표 8 (v0.9 임시값 종료)

`medicalValidityMonths()` 신규. 검사일 기준 연령으로 갈립니다.

- 1종: 12개월 (40세 이상 + 운항형태 commercial → 6개월)
- 2종: 40세 미만 60 / 40대 24 / 50세 이상 12
- 3종: 40세 미만 48 / 40대 24 / 50세 이상 12

생년월일이 없으면 **가장 짧은 기간**으로 계산하고 폼에 "계정정보에 생년월일을 넣으면 정확해집니다"를 띄웁니다.
생년월일·운항형태 입력은 계정정보에 추가했습니다.

## 6. 커런시 운항형태 토글

`computeFlightReadiness()`에 `operationType` 옵션. commercial이면 최근 비행경험 180일 → **90일**.
커런시 대시보드 문구가 `recency.windowDays`로 바뀌어 하드코딩 "180일"이 사라졌습니다.
경량·초경량 트랙은 야간 필드를 저장하지 않고(야간비행 금지) 커런시 탭도 두지 않습니다.

## 7. 비행기록 입력 폼

- 트랙 칩(항공기/경량/초경량) — 기본값은 현재 보는 트랙
- 장치 종류 선택(경량 5종 / 초경량 8종) — 자격 한정 단위라 응시경력 계산에 필요
- 시뮬 모드에 **모의비행훈련장치 구분(FFS/FTD/BATD)** — 별표 4 인정 상한이 장치별로 다름
- 별표 4용 세부 필드 자리 마련: 단독, 감독 하 기장임무, 단독 야외, 구간거리, 야간 이륙
  (타입에는 들어갔고 폼 노출은 다음 단계)

## 아직 안 한 것

1. **응시경력 진척도 UI** — `lib/eligibility/rules.ts` 엔진은 있고 필드도 준비됐지만, 항공기 트랙의
   "자가용 40시간 중 32시간" 식 패널은 아직 안 붙였습니다. 다음 작업.
2. **폼의 별표 4 세부 필드 노출** — 단독·감독하·구간거리 입력칸을 "상세 입력"에만 접어서 넣을 예정.
3. **엑셀 이관의 트랙 자동 분류** — 지금은 이관 시점의 현재 트랙으로 일괄 태그됩니다. 울진 파일은 항공기라 문제없지만,
   섞인 파일이 들어오면 기종 열로 추정하도록 보강 필요.
4. **기관 대시보드 트랙 표시** — schema10 적용 후 구성원 현황에 보유 트랙 열 추가.
5. **Remote ID 인터페이스** — 법제화 전이라 인터페이스 자리만. `vehicleKind`가 무인이면 "참고·보조 자료" 안내가 뜨는 것까지.

## 배포 절차

1. zip 덮어쓰기 → 커밋 → 푸시 (SQL 없이 바로 동작)
2. (선택) Supabase SQL Editor에서 `schema10-pilot-tracks.sql` 실행 — 기관 대시보드 트랙 노출용
3. 배포 후 확인: 계정정보에서 트랙 2개 체크 → 로그북 히어로에 전환 탭이 보이고, 드론 트랙 진척도에 훈련기 시간이 안 잡히는지
