# AWOS v9 — 조종사 전용 정리 (v8 Supabase 전환 포함)

※ v8을 아직 배포 안 했다면 이 v9 하나만 배포하면 됩니다(누적 포함).
※ Supabase 준비(SQL 실행, Confirm email OFF)는 완료된 상태 기준입니다.

## 조종사 전용으로 걷어낸 것

- 랜딩: 기관 대시보드 미리보기 섹션 + 도입 문의 폼 제거
  → 마무리 CTA("오늘 비행부터 기록해 보세요")로 교체
- 상단 메뉴: DASHBOARD / Flight Radar 링크, "기관 도입 문의" 버튼 제거
- 푸터: 기관 문구·링크 제거, 조종사 로그북 설명으로 교체
- 회원가입: 개인/기관 선택 + 직군 5종 선택 제거 → 이름·이메일·비밀번호·소속(선택)만
  (조종사 계정으로 고정. 타 직군·기관 가입은 다음 단계에 별도 오픈)
- 커런시 탭: "내 상태를 소속 기관에 공유" 버튼 숨김
  (코드는 플래그로 보존 — B2B 재개 시 SHOW_ORG_SHARE=true 한 줄로 복원)

유지: 교관 전자서명, TS 연동 카드, GO/NO-GO (조종사 개인 가치)
/dashboard 라우트 자체는 살아있음(링크만 제거) — 기관 데모 필요 시 URL 직접 접근 가능.

## 적용

zip 풀기 → 전부 복사 → awos 폴더 "모두 적용"+"대치" → Commit `v9` → Push

## 배포 후 필수 테스트 (v8 백엔드 전환 검증 겸)

1. 사이트에서 회원가입 (새 계정, 기존 Danny 계정 아님) → 즉시 로그인되는지
2. 퀵 기록 1건 저장 → 새로고침 후 유지되는지
3. Supabase → Table Editor → board_posts에 행 생겼는지 (독립의 증거)
4. 랜딩에 기관 섹션이 사라지고 마무리 CTA가 보이는지
5. 가입 화면이 단순해졌는지
