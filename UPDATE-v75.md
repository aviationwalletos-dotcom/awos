# UPDATE v2.9y — 야외비행 포함 관계 (2026-09-14)

v2.9x 까지 포함해요. **SQL 없어요.** 계산은 안 바뀌고 표시만 바뀌어요.

---

## 무엇이 문제였나

"비행 조건별 시간" 칸이 여섯 개 나란히 있어요.

```
주간   야간   크로스컨트리
야간 크로스컨트리   실제계기   모의계기
```

그런데 **의미가 섞여 있어요.**

| 관계 | 칸 |
|---|---|
| 서로 배타(더하면 총시간) | 주간 ↔ 야간 |
| 서로 배타 | 실제계기 ↔ 모의계기 |
| **포함 관계** | 크로스컨트리 ⊃ **야간 크로스컨트리** |

**여섯 칸 중 하나만 포함 관계인데 똑같이 생겼어요.**

별지 제36호 서식은 주간 야외·야간 야외를 **나눠서** 적게 돼 있어요. 그래서 서식만 본
사람은 앱에서도 나눠 넣는 거라고 생각하기 쉬워요. 그렇게 넣으면 **야외 시간이
야간만큼 적게 잡혀요.**

### 앱의 실제 규칙
```ts
nightCrossCountryOf() = min(입력, 야외, 야간)   // 야외를 넘을 수 없다
dayXc = min(야외 − 야간야외, 주간)              // 주간 야외는 빼서 만든다
```
**크로스컨트리 = 총 야외(주간+야간)** 이고 야간은 그 부분집합이에요.

### 왜 안내로는 부족했나
설명이 **야간 칸 도움말에만** 있었어요 — *"크로스컨트리 중 야간에 한 시간"*.
크로스컨트리 칸을 먼저 채우는 사람은 볼 일이 없어요. 그 칸의 도움말은
"크로스컨트리가 무엇인가"만 설명하고 **"야간도 합쳐 넣어라"는 말이 없었어요.**

---

## 고친 것

### ① 라벨에 드러냄
```
크로스컨트리(시간)        → 크로스컨트리(시간, 야간 포함)
야간 크로스컨트리(시간)   → 야간 크로스컨트리(시간, 위 시간 중)
```
도움말을 열지 않아도 관계가 보여요.

### ② 도움말 앞머리에 관계부터
> **야간에 한 부분까지 합쳐서 전부 넣어 주세요.** 아래 "야간 크로스컨트리"는 이 시간 중
> 야간만 따로 적는 칸이에요(주간·야간을 나눠 적는 게 아니에요). 주간 야외는 앱이 빼서 계산해요.

정의(운항기술기준 43)와 응시경력 구간 조건은 그 아래로 내렸어요.

### ③ 숫자로 확인 — 이게 제일 확실해요
야간 크로스컨트리 칸 아래에 나눠진 결과를 보여줘요.

```
주간 야외 2.3 · 야간 야외 0.7 (합계 3.0)
```

잘못 넣으면 합계가 자기가 생각한 값과 달라서 **바로 눈에 띄어요.**
별지 36호에 실제로 인쇄될 값이기도 해요.

---

## 계산은 그대로예요

`nightCrossCountryOf`·`dayXc` 는 **한 줄도 안 건드렸어요.** 이 규칙은 한국항공대 증명서
(야간 야외 28.6)와 탈론 798건으로 이미 검증된 값이에요(2026-09-11).

이번 변경은 **사람이 잘못 넣는 걸 막는 쪽**이에요.

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    178 통과
npm run build                     통과
```

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. 비행기록 입력에서 크로스컨트리·야간 크로스컨트리를 넣어 보고
   아래 **"주간 야외 · 야간 야외 (합계)"** 줄이 맞게 나오는지 확인
