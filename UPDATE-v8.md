# AWOS v8 — 백엔드 독립 (Supabase 전환) 🚀

**이번 업데이트로 aiapp에서 완전히 독립합니다.** 로그인·회원가입·모든 데이터 저장이
대표님 소유의 Supabase(awos 프로젝트, 서울 리전)로 이동합니다.

## ⚠️ 배포 전에 Supabase에서 할 일 2가지 (순서대로, 5분)

### ① SQL 2탄 실행
1. supabase.com → awos 프로젝트 → 왼쪽 **SQL Editor**
2. 이 패키지의 `supabase/schema2-boards.sql` 파일을 열어 **전체 복사** → 붙여넣기 → **Run**
3. "Success" 확인 (Table Editor에 board_posts, board_comments 추가됨)
※ 1탄(schema.sql)을 아직 실행 안 했다면 그것 먼저 실행하세요. 동봉되어 있습니다.

### ② 이메일 확인 끄기 (가입 즉시 로그인되게)
1. 왼쪽 메뉴 **Authentication** → **Sign In / Up** (또는 Providers)
2. **Email** 클릭 → **"Confirm email"** 토글을 **OFF**
3. Save
※ 이걸 안 끄면 가입자가 메일 인증을 해야 해서 훈련생 씨딩에 큰 마찰이 됩니다.
   나중에 사용자가 많아지면 다시 켜고 SMTP를 붙이면 됩니다.

## 그다음 평소대로 배포

압축 풀기 → 전부 복사 → awos 폴더 "모두 적용"+"대치" → GitHub Desktop
→ Summary `v8 Supabase 전환` → Commit → Push origin

## ⚠️ 꼭 알아야 할 것: 계정이 새로 시작됩니다

- **기존 aiapp 계정(Danny 등)으로는 로그인이 안 됩니다.** 새 서버니까요.
- 배포 후 **회원가입부터 다시** 해주세요 (같은 이메일 사용 가능).
- 기존에 서버에 있던 데이터(친구 기록 1건, Danny 기록)는 새 서버에 없습니다.
  - 필요하면: 배포 **전에** 기존 사이트에서 CSV 백업 → 배포 후 새 계정에서 엑셀 가져오기.
  - 대표님은 어차피 탈론 원본을 새로 넣기로 했으므로 그대로 진행하면 됩니다.

## 배포 후 테스트 체크리스트

- [ ] 회원가입 (개인/조종사/소속기관 입력) → 즉시 로그인되는지
- [ ] 퀵 기록 1건 저장 → 새로고침해도 남아있는지
- [ ] Supabase 대시보드 → Table Editor → board_posts에 행이 생겼는지 (눈으로 확인!)
- [ ] 로그아웃 → 재로그인 → 기록이 그대로인지
- [ ] 자격증 1건 등록 → 새로고침 확인

## 기술 요약 (참고)

- 전환 방식: 31개 API 훅을 그대로 두고, fetch 자리에 aiapp API를 흉내내는
  Supabase 어댑터(baasFetch)를 삽입 — 호출부 무수정 전환 (strangler 패턴 1단계)
- 인증: Supabase Auth (이메일+비밀번호). 토큰 만료 시 자동 갱신 내장.
- 보안: 게시글 조회는 로그인 사용자만(구 aiapp은 완전 공개였음 → 강화),
  작성/수정/삭제는 본인만 (RLS 서버 강제). 도메인별 완전 잠금은 2단계(정규화)에서.
- netlify.toml의 aiapp 프록시는 이제 사용되지 않는 죽은 경로입니다(무해, 추후 정리).
