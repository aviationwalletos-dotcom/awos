# UPDATE v2.9z — 야외비행은 시간만 넣고 앱이 나눠요 (2026-09-14)

v2.9y 까지 포함해요. **SQL 없어요.** 계산 규칙은 안 바뀌고 입력·표시 방식만 바뀌어요.

---

## 무엇이 문제였나

조건별 시간 칸 여섯 개 중 **하나만 포함 관계**인데 똑같이 생겼어요.

```
주간 ↔ 야간            서로 배타
실제계기 ↔ 모의계기     서로 배타
크로스컨트리 ⊃ 야간 크로스컨트리   ← 포함
```

별지 36호는 주간 야외·야간 야외를 **나눠서** 적게 돼 있어요. 그래서 서식만 본 사람은
앱에서도 나눠 넣는 줄 알고 야외 시간을 야간만큼 적게 넣기 쉬워요.

v2.9y 에서 라벨과 도움말로 설명해 봤는데, **설명으로 때우는 방식**이라 안 읽으면 그만이에요.
직관적이지 않다는 지적을 받고 방식을 바꿨어요.

---

## 바뀐 것

### 입력 — 야외 시간 하나만
```
크로스컨트리(시간)  [ 3.0 ]
주간 야외 2.3 · 야간 야외 0.7   수정하기
```

사용자는 **야외 시간만** 넣어요. 주간·야간은 앱이 나눠서 아래에 보여줘요.
숫자로 보이니까 잘못 넣으면 바로 눈에 띄어요.

**"수정하기"** 를 누르면 야간 야외 칸이 나와요. 자동이 안 맞는 비행에만 쓰면 돼요.

### 요약 — 두 칸을 하나로
```
전:  크로스컨트리 32.4    야간 크로스컨트리 1.6     ← 더해야 하는 값처럼 보임
후:  야외비행 32.4
     주간 30.8 · 야간 1.6
```

총계를 남긴 이유는 **별표 4 응시경력 요건이 야외 총 시간을 보기 때문**이에요.

---

## 자동 규칙과 그 한계

```
야간 야외 = min(야간 시간, 야외 시간)
주간 야외 = 야외 − 야간 야외
```

한국항공대 증명서(야간 야외 28.6)와 탈론 798건으로 검증된 규칙이에요(2026-09-11).
**한 줄도 안 바꿨어요.**

다만 이 규칙은 *"야간 시간은 전부 야외비행 중에 있었다"* 고 가정해요. 안 맞는 경우가 있어요.

> 주간에 야외 2.0 → 착륙 없이 야간 장주 1.0
> 자동: min(1.0, 2.0) = 야간 야외 1.0 ← 틀림. 야외는 전부 주간이었음

이래서 **"수정하기"를 남겼어요.** 없으면 이런 기록을 고칠 방법이 없어요.

이미 야간 야외를 직접 넣어둔 기록은 **펼친 채로 열려요.** 값이 조용히 덮이면 안 되니까요.

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    184 통과 (178 + 신규 6)
npm run build                     통과
npx playwright test --list        20건
```

새 테스트 6건은 **입력·요약·PDF 가 같은 숫자를 보는지** 고정해요 —
야간<야외 / 야간>야외 / 야간 없음 / 직접 입력 / 상한 / `주간+야간=야외` 항등식.

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. 비행기록 입력에서 야간 1.0, 크로스컨트리 3.0 을 넣고
   아래에 **주간 야외 2.0 · 야간 야외 1.0** 이 뜨는지
3. **수정하기** 를 눌러 칸이 나오는지
4. 요약 카드가 **야외비행 하나**로 합쳐졌는지
