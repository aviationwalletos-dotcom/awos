# UPDATE v22 — 화면 정리 5건 (2026-09-03)

1. **이름 위치** — "조종사 · 오재헌님"을 AWOS 배지 아래(왼쪽)로. 오른쪽엔 자격 구분 전환 버튼만.
2. **구글 본인 인증 반복** — 앱 코드에서 재인증을 강제하는 파라미터(prompt/max_age)는 없습니다. 스크린샷의 "기기 확인(숫자 5)"은 Google 계정의 2단계 인증이며 Google이 브라우저를 신뢰하지 않을 때 뜹니다. 앱 쪽 조치 없음 — 아래 답변 참고.
3. **날짜 달력 다크 테마** — `input[type=date/time]`에 `color-scheme: dark`. 브라우저 기본 달력이 어두운 배경에 묻히던 문제.
4. **요청함 기본 필터 '대기중'** — 자격증·신체검사 / 교관 승인 / 비행경력증명서 승인 세 패널 모두.
5. **[BUGFIX] 초경량 기체 선택 오류** — 기체 목록이 폼보다 늦게 로드되면 선택값이 비어 있는데 select는 첫 기체를 보여줘서 "고른 것처럼 보이는데 저장이 막히는" 문제. 목록이 오면 첫 기체로 자동 맞춤.
