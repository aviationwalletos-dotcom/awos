# UPDATE v2.9s — 조종교육증명 등급 표시 (2026-09-14)

v2.9r 까지 포함해요. **SQL 없어요.**

---

## 무엇이 문제였나

자격 카드의 "IV. 한정사항(RATINGS)" 줄에 조종교육증명이 이렇게 나왔어요.

```
조종교육증명 선임(비행기)      ← 육상단발/육상다발이 없음
조종교육증명 초급(비행기)
```

실물 자격증에는 **"초급(비행기/육상단발, 비행기/육상다발)"** 처럼 등급까지 적혀 있어요
(별표 4 한정심사).

## AI 잘못이 아니었어요

읽는 쪽은 멀쩡했어요.

| 단계 | 상태 |
|---|---|
| AI 스키마 | `flightInstructorRatings[].classRating` 로 등급을 물어봄 ✅ |
| 선택지 | `CFI_SENIOR_AIRPLANE_SEL` 등 등급별 6개 + `(등급 미상)` 2개 ✅ |
| 매핑 | `선임 조종교육증명 - 비행기 육상단발` 로 이름을 만듦 ✅ |
| **카드 표시** | **등급을 버림** ❌ |

`MyCertificateStatusCard.tsx` 의 `ratingLine()` 이 종류(비행기/헬리콥터)만 뽑고 있었어요.

```ts
const kind = n.includes('헬리콥터') ? '헬리콥터' : '비행기'
return [`조종교육증명 ${grade}(${kind})`]
```

바로 위의 `'한정'` 분기는 `n.includes('육상다발')` 로 등급을 제대로 뽑아요.
**조종교육증명만 그 처리를 안 받았어요.**

그리고 같은 함수의 주석이 **맞는 형식을 이미 적어 놨어요** —
`예: … 조종교육증명 초급(비행기/육상단발)`. 코드가 자기 주석과 달랐어요.

## 고친 것

```
조종교육증명 선임(비행기/육상단발)
조종교육증명 초급(비행기/육상다발)
조종교육증명 초급(헬리콥터)          ← 헬리콥터는 등급 구분 없음
조종교육증명 선임(비행기)            ← 등급 미상이면 예전처럼
```

수상단발/수상다발도 같이 처리해요.

테스트 7건 추가(등급 4종 · 등급 미상 · 헬리콥터 · 다른 자격 회귀).

---

## ⚠️ 화면만 고쳐서는 안 될 수도 있어요

카드는 **자격증에 저장된 이름**을 보고 등급을 뽑아요. 그래서 **등록할 때 등급이 안 들어갔으면
여전히 "(비행기)"로 나와요.**

조종교육증명을 등급별로 나눠 등록하는 건 **2026-09-13 에 들어간 기능**이에요.
그 전에 등록한 자격증은 이름에 등급이 없어요.

### 확인하는 법
자격증 탭에서 조종교육증명을 열어 **이름**을 보세요.

| 이름 | 뜻 | 조치 |
|---|---|---|
| `선임 조종교육증명 - 비행기 육상단발` | 데이터 정상 | 배포하면 카드에 바로 나와요 |
| `선임 조종교육증명 - 비행기` | 등급이 안 들어감 | 수정해서 세부 종류를 등급별로 고르거나, 지우고 다시 읽히세요 |

배포 뒤 카드에 **줄이 4개로 늘면**(초급 2 + 선임 2) 데이터도 정상이라는 뜻이에요.
2개 그대로면 데이터 쪽을 손봐야 해요.

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    159 통과 (152 + 신규 7)
npm run build                     통과
npx playwright test --list        20건
```

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. 카드의 조종교육증명 줄에 **육상단발/육상다발**이 나오는지 확인
3. 안 나오면 위 표대로 자격증 이름을 확인해 주세요
