# UPDATE v16 — 초경량 기체 카드 · 로그기록지 입력 · 비행경력증명서 출력 (2026-09-03)

붙임2 비행 로그기록지와 두 운영세칙의 별지 제2호(비행경력증명서) 서식을 앱에 옮겼습니다.
경량항공기·항공기 폼은 손대지 않았습니다(별도 서식이 없고 현재 폼이 이미 맞음).

검증: 타입 0 · 린트 0 · 테스트 26 → **31건** · 빌드 통과.

## 1. 기체 카드 — `types/vehicle.ts`, `hooks/useVehicles.ts`, `components/logbook/VehicleCards.tsx`

증명서에 기록마다 적어야 하는 종류·형식·신고번호·최종인증검사일·자체중량·최대이륙중량은 기체별 고정값이라
한 번만 등록하고 기록이 참조합니다. 초경량 구분의 "기록 입력" 탭 상단에 "내 기체"가 생겼습니다.

- 최대이륙중량을 넣으면 **무인 종(1~4종) 자동 판정** (별표 2: >25kg 1종 / >7 2종 / >2 3종 / >250g 4종)
- 안전성인증 면제 체크 → 증명서에 "면제" 출력
- **인증 유효기간 만료일** → 만료 후 비행은 경력 합산·증명서에서 자동 제외 (기재요령 주의사항 2).
  종이로는 못 하는 검증이라 이 앱의 차별점입니다.
- 저장: localStorage + "개인설정" 게시글 JSON. SQL 없음.

## 2. 로그 필드 추가 — `types/logbook.ts`

`vehicleId`, `flightCount`(무인 비행횟수), `takeoffTime`/`landingTime`, `hourMeterStart`/`hourMeterEnd`,
`pilotingTime.training`(훈련시간 — 세칙 제10조의 세 번째 구분), `flightPurpose`, `instructorLicenceNo`, `traineeName`.

## 3. 초경량 입력 폼 — `components/logbook/UltralightEntryForm.tsx`

로그기록지 ①~⑦ 순서 그대로. 초경량 구분에서는 이 폼이 기존 폼을 대체합니다(편집도 동일).

- 기체 선택 → 종류·형식·신고번호 자동
- 이륙·착륙 시각 → 비행시간(분) 자동 계산, 자정 넘김 처리
- **분으로 입력, 시간으로 저장** — 48분 → 0.8, 둘째자리 버림(기재요령 7). 테스트로 고정
- 임무별 기장·훈련·교관 소계가 비행시간과 다르면 노란 경고
- 무인은 비행횟수·비행장소, 유인(동력비행장치·회전익)은 착륙횟수·FROM/TO — 별지 제2호 두 판본의 차이
- 선택한 날짜에 기체 인증이 만료 상태면 빨간 안내(저장은 되지만 제외됨)

## 4. 비행경력증명서 출력 — `lib/flightExperienceCertificatePrint.ts`

초경량 구분에서 "인쇄" 버튼을 누르면 로그북 대신 **별지 제2호 서식**이 A4 가로로 열립니다(PDF 저장 가능).

- 열 구성·번호(①~⑧)·영문 병기·하단 증명문·발급기관 서명란까지 서식 그대로
- 인증 만료 후 비행은 제외하고 하단에 "제외 N건" 표기
- 우측 상단 **"초안 · 발급기관 확인 전"** 표시. 앱은 발급기관이 아니므로 교육기관 서명·날인 전엔 효력이 없음을 문서 자체에 박아뒀습니다

## 5. 컴플라이언스 — `roleCompliance.ts`

"기체 안전성 인증 유효성" 항목이 info 고정에서 실제 판정으로 바뀌었습니다.
등록 기체 없음 → 안내 / 전부 유효 → 충족 / 만료 기체 있음 또는 제외 비행 있음 → 미충족 + "N건 경력 제외" 배지.

## 남은 것

- 유인 초경량(동력비행장치·회전익)은 무인 폼을 공유하되 착륙횟수·FROM/TO 모드로 동작. 실사용 피드백 후 분리 여부 결정
- 항공기·경량 구분의 비행경력증명서(시행규칙 서식)는 기존 로그북 인쇄 유지. 별도 서식화는 다음
- 지도조종자 성명·서명은 기존 "교관 서명 요청" 흐름을 그대로 씁니다. 자격번호만 새 필드
- Remote ID 자동 기록이 들어오면 이륙·착륙 시각과 아워미터 칸이 자동으로 채워질 자리입니다
