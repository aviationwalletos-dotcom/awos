# AWOS v13 — 컨셉 색·형식 충실 이식 (v10~v12 포함)

기능이 아닌 **디자인 형식**만 컨셉(Landing_Page_3) 원값으로 이식했습니다.

## 색 (컨셉 원값 그대로)

| 항목 | 이전(절충) | v13(컨셉) |
|---|---|---|
| 페이지 배경 | #070B14 | **#05070D** |
| 패널 | #0D1526 | **#0B1220** |
| 시안 포인트 | #22D3EE | **#00D4FF** |
| **행동 버튼** | 시안 솔리드 | **블루 #2563EB** (hover #1D4ED8 + 1px 리프트) |

핵심 발견: 컨셉의 메인 버튼은 시안이 아니라 **블루**입니다. 시안은 포인트 강조 전용,
행동(시작하기 등)은 블루 — 이 역할 분리가 컨셉이 고급스러워 보이는 비결이라 그대로 채택.
보조 버튼은 컨셉의 고스트 형식(헤어라인 테두리, hover 시 시안 테두리)으로.

## 형식

- 버튼·입력 라운드 6px (컨셉 값)
- 트랜지션 이징 cubic-bezier(.16,1,.3,1) 변수 추가
- v11의 브래킷·syscell·스트립·모노 아이브로우·격자 배경은 그대로 유지
- PWA 테마 컬러·매니페스트 동기화

※ 참고: #05070D+형광 시안 조합은 예전 리뷰에서 "글자 번짐" 지적이 있었던 조합입니다.
  이번엔 대표님 선호를 따랐고, 시안을 포인트 전용으로 절제해 부담을 줄였습니다.
  실화면에서 번져 보이면 본문 시안 사용처만 미세 조정 가능합니다.

## 적용

zip 풀기 → 전부 복사 → awos 폴더 "모두 적용"+"대치" → Commit `v13` → Push
