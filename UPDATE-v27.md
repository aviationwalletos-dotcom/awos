# UPDATE v27 — 승인 대기 고착 버그 · 한정 추가 통합 (2026-09-03)

검증: 타입 0 · 린트 0 · 테스트 32 · 빌드 통과.

## [BUGFIX] 관리자가 승인해도 "승인 대기"로 남던 문제

원인은 `useCertificates.updateCertificate`의 **오래된 클로저**입니다.
자격증을 등록하면 ① 로컬에 추가 → ② 사진 업로드·요청 게시글 생성(`await`) → ③ 게시글 ID를 자격증에 저장하는데,
③의 `updateCertificate`가 ①보다 **이전 렌더의 certificates 목록**을 읽어서 방금 추가한 자격증을 못 찾고 조용히 `return` 했습니다.
게시글 ID가 저장되지 않으니 승인 감시(Watcher)가 아예 마운트되지 않았고, 관리자 화면에는 게시글이 정상 도착해 "승인됨"으로 보였습니다.

- `updateCertificate`가 항상 최신 목록을 ref로 읽도록 수정
- **이미 고착된 자격증 복구**: `CertificateApprovalLinkRepair`가 자격증 탭에서 "대기 중인데 게시글 ID 없는" 자격증을 찾아
  요청 게시판 제목의 certId로 게시글을 다시 연결합니다. 스크린샷의 4건은 배포 후 자격증 탭을 열면 자동으로 승인 상태로 바뀝니다.

## 한정 추가 — 한 번에 선택

구분 "한정 추가"를 고르면 종류·등급을 따로 넣는 대신:
1. **어느 자격증명에 추가하나요?** — 보유한 조종사 자격증명 목록(자격번호 포함)
2. 종류 (비행기/헬리콥터)
3. 등급 (SEL/MEL/SES/MES, 비행기일 때만)
4. 형식 (있으면, 예: B737)

명칭 자동 생성 예: `사업용 조종사(CPL) · 비행기 · 육상다발 · 형식한정(B737)`
`Certificate.linkedCertificateId`·`typeRating` 추가. 마스터 카드의 한정사항에 `비행기/육상다발, 형식한정(B737)` 형태로 붙습니다.
자격증명이 아직 없으면 "먼저 조종사 자격증명을 등록해 주세요" 안내.
