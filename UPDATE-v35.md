# UPDATE v35 — [핵심 버그] 교관 서명이 한 번도 인정되지 않던 문제 (2026-09-04)

## 원인
승인 교관 집합(`fetchApprovedInstructorIds`)은 교관 승인 신청서 **제목의 userId = 이메일**로 만들어졌고,
서명 댓글의 `author_id`는 **auth uuid**입니다. `findSignedComment()`가 이메일 집합에서 uuid를 찾으니
**어떤 서명도 일치한 적이 없었습니다.** 서명 댓글은 정상 등록되는데(서버에 남아 있음) 판정만 실패해
화면은 영원히 "대기중", 캔버스는 비워지고(성공 경로), 오류도 없었습니다.

본인이 본인 기록에 서명한 것과는 무관합니다(그 경우도 정상 인정됩니다).

## 수정
- 게시글 목록 항목에 `author_id`(uuid) 포함
- 승인 교관 집합에 **이메일 + 신청 게시글 작성자 uuid**를 함께 저장 → 댓글 author_id(uuid)와 일치
- 회귀 테스트 3건 추가(`signatureRequest.test.ts`): uuid 집합에서 인정 / 이메일만 든 집합에서 미인정(옛 버그 재현) / data: URL 파싱

## 배포 후
이미 남아 있는 [SIGNED] 댓글들은 **다시 서명할 필요 없이** 다음 로드 때 인정됩니다. 서명 요청함의 대기 3건은 "완료됨"으로, 학생 기록 상세에는 교관 서명이 나타납니다.
