# UPDATE v2.9l — 이월 기록 배지 제거 (2026-09-13)

v2.9k 까지 포함해요. **SQL 없어요.** 한 줄짜리 표시 수정이에요.

---

## 무엇이 문제였나

비행경력증명서 이월 기록(누적 합계) 카드에 개별 비행용 배지가 붙고 있었어요.

```
2026-03-16   [FTD]
비행경력증명서(누적 기록)
블록타임 1021.9시간
```

배지는 **"이 비행 한 건이 어떤 비행이었나"** 를 뜻해요. 그런데 이월 기록은 과거 수백 건의
합계라 그 질문 자체가 성립하지 않아요.

특히 FTD 규칙이 **이른 반환**이에요.

```ts
if ((entry.groundTrainerTime ?? 0) > 0) return ['FTD']   // 다른 배지를 다 지움
```

합계 안에 시뮬레이터가 **몇 시간만** 섞여 있어도 1,000시간짜리 카드 전체가 FTD 로 찍혀요.
화면에는 *"천 시간을 전부 시뮬레이터로 탔다"* 로 읽혀요. 하필 비행경력증명서 기록이라
오해가 제일 크게 나는 자리예요.

오른쪽 카드(69.9시간 · X-C + NGT)도 같은 문제예요. "누적 안에 야외비행도 야간도 있다"는
뜻인데 배지는 "이건 야간 야외비행이었다"로 읽혀요.

## 무엇을 했나

`EntryList.tsx` 의 `deriveBadges()` 에 한 줄을 더했어요.

```ts
if (entryTrack(entry) === 'ultralight') return []
if (entry.origin === 'flight_experience_certificate') return []   // ← 추가
if ((entry.groundTrainerTime ?? 0) > 0) return ['FTD']
```

초경량을 빼는 것과 같은 이유·같은 방식이에요. 코드 주석에 왜 빼는지 적어 뒀어요.

**배지를 빼도 구분이 흐려지지 않아요.** 카드에 이미 제목 **"비행경력증명서(누적 기록)"** 과
칩 **"비행경력증명서 · 인증완료"** 가 있어요. 오히려 지금은 배지가 그 맥락을 덮고 있었어요.

## 숫자에는 영향 없어요

`deriveBadges()` 는 **`EntryList` 전용**이에요. 커런시·응시경력·승무시간·PDF 어디에도
들어가지 않아요. 표시만 바뀌어요.

`origin` 값 셋 중 `legacy_excel`(엑셀로 가져온 기록)은 **실제 개별 비행**이라 배지가 맞아요.
그대로 둬요.

## 언제부터 있었나

오늘 바꾼 것 때문에 생긴 게 아니에요. 원래 있던 건데, v2.9g 에서 배경색이 제대로 나오기
시작하면서 눈에 띈 거예요.

---

## 참고 — "로그인이 필요합니다" (조치 안 함)

계정 화면의 "교관 승인 신청" 칸에 이 오류가 떴다가, 로그아웃 후 재로그인으로 해결됐어요.

원인은 **죽은 세션**이에요. `requireClient()` 가 토큰을 못 찾으면 이 메시지를 던져요.
카카오 계정 연결·전환 과정에서 옛 토큰이 브라우저에 남아 있었던 걸로 보여요.

작은 틈이 하나 보여요 — 그 칸의 **"다시 시도"** 버튼은 이 경우에 도움이 안 돼요. 세션이
죽었으면 재시도해도 같은 결과라, 재로그인을 안내하는 게 맞아요. 다만 계정 연결·전환이라는
드문 상황에서만 나오고, 미팅 전에 손댈 만큼 급하진 않아 그대로 뒀어요.

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    114 통과
npm run build                     통과
npx playwright test --list        20건
```

`package.json` · `package-lock.json` 안 건드렸어요.

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. 배포 뒤 로그북에서 **비행경력증명서(누적 기록) 카드에 배지가 사라졌는지** 확인
3. 일반 비행 기록에는 배지가 **그대로 있는지** 같이 확인

삭제된 파일 없어요.
