# UPDATE v2.9r — 경량 자격증 인식 + 초경량 검증 (2026-09-13)

v2.9q 까지 포함해요. **SQL 없어요.**

---

## 왜 이걸 했나

v2.9o 에서 초경량을 고치면서 **테스트를 안 붙였어요.** tsc·build 는 통과했지만
"실제로 동작하는지"는 확인 못 한 상태였어요. 확인해 보라는 지적을 받고 점검했고,
그 과정에서 **경량에도 똑같은 구멍**이 있는 걸 찾았어요.

---

## 1. 초경량 — 검증했어요 ✅

단위 테스트 7건을 붙였어요. 대표님 실제 자격증(91-180070, 무인멀티콥터/1종)을 그대로 썼어요.

| 확인한 것 | 결과 |
|---|---|
| 무인멀티콥터 **1종**까지 채우는가 | ✅ |
| 종을 못 읽으면 **비우는가**(없는 값을 만들지 않는다) | ✅ |
| 종이 있는 4종류(비행기·헬리콥터·멀티콥터·수직이착륙기) 매핑 | ✅ |
| 유인 장치는 종 없이 | ✅ |
| **모르는 코드면 제안 안 함**(엉뚱한 자격을 만들지 않는다) | ✅ |
| 중복 판정 | ✅ |
| 항공기용 한정·계기·EPTA 가 **안 섞임** | ✅ |

카테고리 이름(`초경량비행장치 조종자증명`)과 세부 종류 매핑(`ULTRALIGHT_CERT_TYPES`)도
실제 정의와 대조했어요. 맞아요.

---

## 2. 경량 — 같은 구멍이 있었어요 ❌ → 고침

```
netlify/functions/read-document.mjs   lsaKind 없음
src/lib/certificateSuggestions.ts     LSA 분기 없음
```

경량항공기 조종사 자격증명도 `licenceCode` 가 PPL/CPL/ATPL/MPL 이 아니라서,
초경량과 똑같이 `if (!code) return out` 에서 빠져나갔어요. **제안이 하나도 안 만들어져요.**

### 고친 것
**① AI 스키마에 `lsaKind` 추가** — 5종(시행규칙 제113조)
```
LSA_AIRPLANE(타면조종형비행기) | LSA_WEIGHT_SHIFT(체중이동형비행기)
LSA_HELICOPTER(경량헬리콥터) | LSA_GYROPLANE(자이로플레인) | LSA_POWERED_PARACHUTE(동력패러슈트)
```

**② 매핑을 종류별로 일반화** — 초경량 분기를 `otherLicence` 로 묶어서 경량도 같은 길을 타요.
따로 복사한 코드가 아니라 한 곳이에요. 다음에 종류가 늘어도 여기만 고치면 돼요.

**③ 안내도 경량까지** — 조종사 화면에서 경량 자격증을 읽으면 이렇게 떠요.

> **경량항공기 조종사 자격증이에요**
> 읽은 자격증은 **경량항공기 조종사 - 타면조종형비행기** 예요. 지금은 조종사 자격을 등록하는 중이라 값을 채우지 않았어요.
> `[경량항공기 조종사로 옮기기]`

테스트 6건 추가. 5종 전부, 모르는 코드, 중복, 항공기용 제안 미혼입까지요.

---

## 3. 회귀 확인

초경량·경량 분기를 앞에 넣었으니 **항공기 경로가 안 깨졌는지**도 테스트로 고정했어요.
CPL 한 장에서 자격증명 + 한정 + 계기 + 교관 + EPTA 가 예전처럼 다 나와요.

---

## 세 종류 정리

| 자격 종류 | AI 필드 | 카테고리 |
|---|---|---|
| 조종사 | `licenceCode` (PPL/CPL/ATPL/MPL) | 조종사 자격증명 |
| 경량항공기 조종사 | `lsaKind` (5종) | 경량항공기 조종사 자격증명 |
| 초경량비행장치 조종자 | `ultralightKind` (12종) + `uasGrade` (1~4종) | 초경량비행장치 조종자증명 |

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    152 통과 (138 + 신규 14)
npm run build                     통과
npx playwright test --list        20건
node --check netlify/functions/*  통과
```

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. **Netlify 함수가 또 바뀌었어요** — 배포 뒤 AI 읽기 한 번
3. 확인
   - 초경량 화면에서 무인멀티콥터 자격증 → **무인멀티콥터 + 1종**
   - 조종사 화면에서 읽으면 → 안내 + 옮기기 버튼
   - 경량 자격증이 있으시면 같은 흐름으로 한 번

경량은 실물 자격증으로 확인을 못 했어요. 혹시 갖고 계시면 한 번 읽혀 봐 주세요.
