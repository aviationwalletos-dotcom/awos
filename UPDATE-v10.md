# AWOS v10 — 피드백 반영 라운드 1

받은 피드백 목록 중 이번에 반영한 것과 다음(v11)으로 넘긴 것을 구분했습니다.

## ✅ 이번에 반영 (v10)

1. **Flight Radar 완전 제거** — 라우트까지 삭제, 단일 AWOS 경험
2. **엑셀 가져오기 "항상 뜨던 오류 2건" 원인 제거** — 날짜·기종·구간이 모두 없는
   하단 합계/서명/빈 행을 오류로 세지 않고 자동 제외. 미리보기에 안내문 추가.
3. **계기접근 안내** — 울진 서식처럼 계기접근 컬럼이 없는 엑셀은 0으로 저장됨을
   가져오기 화면에 명시 (가져온 뒤 상세 편집으로 보완 안내)
4. **비행경력증명서 PDF 업로드 지원** — 이미지+PDF 모두 가능, PDF는 파일명 배지로 표시
5. **증명서 업로드 무한 로딩 버그 수술** — 원인: await 이후 리액트 이벤트 객체 접근으로
   예외 발생 시 로딩 해제 코드 미도달. 폼 참조 선캡처 + try/finally로 구조적 차단.
6. **문구 전환: "기관" → "관리자"** — 인증 요청·승인·반려 관련 안내문 전부
   (기관 계정이 사라진 현 구조에 맞게. 발급기관 등 정당한 표현은 유지)
7. **자격증 탭: 보유 목록을 최상단으로** — 목록 → TS연동 → 등록 폼 순서
8. **퀵 기록 즐겨찾기 칩** — 저장할 때마다 공항·기종을 자동 학습해 입력칸 아래
   칩으로 표시, 탭 한 번에 입력 (수동 등록 불필요, 기기별 저장)
9. **커런시 규칙 세트 v0.9 표기** — 원문 대조 검증 예정임을 판정 캡션에 명시

## ⏭ 다음 라운드(v11)로 — 관리자 콘솔 중심

- **관리자 콘솔**: 관리자 전용 페이지(사용자 목록 + 비행경력증명서/자격증/신체검사
  3분할 인증 요청함, 승인·반려). 이 포맷이 나중에 기관 계정 화면으로 재사용됨.
  ※ v10 시점에는 증명서 인증 요청이 접수만 되고 승인 처리 화면이 없습니다(콘솔에서 해결).
- **공식 양식(상세 입력) 스마트화**: 단발육상 1.5 입력 → 비행구분 따라 Dual/PIC·총시간
  자동, 주간 1.3 → 야간 0.2 자동 보완, 즐겨찾기 칩을 상세 폼에도
- **퀵 기록 존폐 결정**: 상세 폼이 충분히 빨라진 뒤 통합 여부 판단 (아래 의견 참조)
- 자격증 사진 업로드+관리자 인증, 신체검사 만료일 자동 계산(규칙 확정 후),
  소속 교관 목록에서 선택해 서명 요청, 커런시 v1.0 조항 근거 확정

## 참고: 업로드해주신 프로토타입 HTML 2종

다른 세션이 만든 화면 시연용 파일(새로고침 시 초기화, 로그인·저장 없음)이라
사이트를 그것으로 교체하지 않고, 담긴 아이디어를 실제 코드에 이식하는 방식으로
반영했습니다. 실사이트가 이미 훨씬 앞서 있습니다(실 백엔드·인증·자동 테스트).

## 적용

zip 풀기 → 전부 복사 → awos 폴더 "모두 적용"+"대치" → Commit `v10` → Push
