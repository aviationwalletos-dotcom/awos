# UPDATE v37 — 사용자·기록 증가 대비 확장성 점검 (2026-09-04)

검증: 타입 0 · 린트 0 · 테스트 35 · 빌드 통과.

"N이 커지면 요청도 커지는 곳"을 코드에서 전부 찾아 고쳤습니다. 아래 표의 **이전**은 실제 코드가 하던 일입니다.

| # | 위치 | 이전 (사용자 U명, 기록 E건, 대기 요청 P건) | 이후 |
|---|---|---|---|
| 1 | **로그북 초기 동기화** | 게시판 **전체 사용자**의 게시글을 100건씩 최대 50페이지(5,000건)까지 내려받은 뒤 클라이언트에서 내 것만 골라냄. U가 늘수록 모든 사용자의 로그인이 느려지고, 전체 5,000건을 넘으면 **내 예전 기록이 잘려서 안 보임** | 서버에서 `author=me`로 내 것만 조회. 자격증·업무기록·개인설정 목록도 동일 |
| 2 | 로그북 배경 동기화(서명·증명서 승인 감지) | 대기 기록마다 워처 컴포넌트 1개 = 댓글 조회 P번 | 배치 1회 (`useCommentsBatch`) |
| 3 | 자격증 승인 감지 | 대기 자격증마다 조회 | 배치 1회 |
| 4 | 관리자 패널 3종(자격증·교관·증명서 승인) | 항목마다 댓글 조회 + **화면 밖 페이지는 상태 미확인** | 배치 1회, 전체 항목 상태 정확 |
| 5 | 승인 교관 명단 계산 | 신청서마다 댓글 조회 | 배치 1회 (v36) |
| 6 | 서명 요청함 | 항목마다 조회 | 배치 1회 (v36) |
| 7 | **본인 서명 이미지** | 캔버스 원본 배율(≈1200×320) PNG 15~30KB를 기록마다 localStorage에 저장 → 기록 200~300건에서 **5MB 상한** 도달 위험 | 480×128로 축소 저장(2~5KB). 서버 게시글도 가벼워짐 |
| 8 | localStorage 저장 실패 | 조용히 무시 | 화면에 안내("오프라인 사본 갱신 실패, 서버에는 저장됨") |

## 이번에 안 건드린 것 (아직 괜찮거나 다음 단계)
- **구성원 현황(관리자)**: 전체 기록 최대 5,000행·게시글 1,000건을 한 번에 읽어 집계. 관리자만 쓰고 지금 규모엔 문제없음. 구성원 50명·기록 1만 건 넘으면 서버 집계(뷰/함수)로 옮겨야 함.
- **로그북 화면 계산(총계·커런시·응시경력)**: 기록 수천 건까지 클라이언트 계산으로 충분(0.1시간 정수 합산, 정렬 1회).
- **게시글 목록 `limit=100` 캡**(서명 요청·승인 요청 등 관리자용 목록): 100건 넘으면 페이지 순회 필요. 현재 요청함은 전체 순회 미구현 → 대기 요청이 100건을 넘기 시작하면 확장.
- **서버 인덱스**: `board_posts(board_id, author_id, created_at)` 복합 인덱스가 있으면 1번이 더 빨라짐. 스키마 파일로 추가 예정(schema11).

## 배포 후 체감
- 로그인 직후 로그북 뜨는 시간: 사용자 수와 무관해짐(내 기록 수에만 비례)
- 서명 요청함·관리자 패널: 항목 수와 무관하게 요청 2번
