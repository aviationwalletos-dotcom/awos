# UPDATE v25 — 한정 통합 · PDF 문구 · 코드 부채 3건 (2026-09-03)

검증: 타입 0 · 린트 0 · 테스트 32 · 빌드 통과.

## 1. "한정"을 자격증명 안으로 (법 구조 검토 결과)

항공안전법 **제37조**: 자격증명은 항공기 종류·등급·형식을 **한정하여** 발급한다. 즉 한정은 별개 자격이 아니라 **자격증명의 속성**입니다.
"사업용 조종사"라는 자격증은 존재하지 않고, "사업용 조종사 · 비행기 · 육상다발"이 하나의 자격증입니다. 지적하신 게 맞습니다.

다만 별표 4 나목(한정심사)이 보여주듯 **한정은 나중에 추가로 딸 수 있고**(예: 육상단발로 PPL 취득 후 육상다발 등급 한정 추가, 형식 한정 추가),
그때는 별도 시험·별도 취득일이 있습니다. 그래서 데이터로는 한정 항목을 남기되 **화면에서는 자격증명 카드 하나로** 합쳤습니다.

- **자격증 등록**: "조종사 자격증명"을 고르면 종류(비행기/헬리콥터)·등급(SEL/MEL/SES/MES)을 함께 받고 명칭이 `사업용 조종사(CPL) · 비행기 · 육상단발`로 자동 생성
- **한정 추가**: 구분 드롭다운에서 "한정 추가 (기존 자격증명에 등급·형식 추가)"로 이름만 바뀜. 저장 값은 그대로 `한정`
- **카드덱**: 한정 카드 제거 → **조종사 덱 6장**. 자격증명 카드에 CPL · 비행기 · MEL · B737 같은 칩이 함께 보임
- `Certificate`에 `aircraftCategory`·`classRating` 필드 추가 — 응시경력 엔진의 prereq(`ppl-airplane` 등) 판정에 쓸 수 있음

계기비행증명(제44조)·조종교육증명(제35조)은 법적으로 별도 증명이라 카드를 유지합니다.

## 2. "인쇄" → "PDF 저장"
버튼·설명·랜딩 문구 전부. iOS는 보류(별도 대응 전까지 새 탭으로 열림).

## 3. 코드 부채
- **ErrorBoundary** — 라우트 전체를 감쌈. 오류 시 흰 화면 대신 메시지 + 새로고침/홈 버튼. 기록은 브라우저에 남아 있다는 안내 포함
- **앱 내 확인 대화상자** — `window.confirm` 3곳(자동분류 확정·기체 삭제 등) 교체. ESC·바깥 클릭으로 닫힘, 위험 동작은 빨간 버튼
- **프로필 설정 중복 조회 제거** — 한 화면에서 4~6번 나가던 "개인설정" 게시글 조회를 사용자당 1번으로(30초 공유 캐시)
