# UPDATE v54 — 로그인 방법 연결·가입 안내 보강 (2026-09-05)

1. **이메일 로그인 연결하기** — 계정정보 "로그인 방법 연결"에 추가. 구글·카카오로만 가입한 계정에 비밀번호를 설정해 이메일+비밀번호로도 로그인 가능(Supabase 비밀번호 설정). 카카오로만 가입해 계정 이메일이 없는 경우는 "계정 이메일로 등록"을 먼저 하도록 안내. 연결 상태 판정에 `app_metadata.providers` 포함.
   - E2E(비밀번호 로그인)에 재헌님 구글 계정을 쓰려면 여기서 비밀번호를 한 번 설정하면 됨.
2. **[BUGFIX] 이미 가입된 이메일로 재가입 시 "인증 메일을 보냈어요"** — Supabase는 이 경우 오류 없이 메일도 안 보낸다(identities 빈 응답). 감지해서 "이미 가입된 이메일이에요 — 인증 메일 다시 보내기 또는 로그인"으로 안내.
3. **이메일 도메인 오타 제안** — 가입·로그인 폼에서 `naer.com`, `gamil.com` 등 흔한 오타를 감지해 "혹시 naver.com 아닌가요? (눌러서 고치기)" 표시.
4. 동일 이메일 자동 연결 — Supabase 기본 동작: 인증된 같은 이메일로 구글 로그인하면 기존 계정에 자동 연결됨(설정 "Allow manual linking" ON 유지).
