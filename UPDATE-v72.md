# UPDATE v2.9v — AI 읽기 전수 점검 (2026-09-14)

v2.9u 까지 포함해요. **SQL 없어요. Netlify 함수가 바뀌었어요.**

---

## 왜 이걸 했나

같은 종류의 실수를 이틀 연속 놓쳤어요(조종교육증명 등급 · 초경량·경량 배열).
하나씩 훑는 방식으로는 계속 놓칠 것 같아서 **전수 대조**로 바꿨어요.

방법: AI 스키마의 필드를 모두 뽑아 **소스에서 실제로 읽는 곳이 있는지** 세고,
자격증 구분 17개가 각각 **AI 경로를 갖고 있는지** 표로 맞춰 봤어요.

---

## ① 신체검사 종류가 한 번도 안 채워지고 있었어요 ⚠️ 제일 큼

스키마에 `medicalClass` 가 **두 번** 정의돼 있었어요.

```js
medicalClass: "종류: '제1종' | '제2종' | '제3종'"   ← 문자열
medicalClass: '항공신체검사 종류(1, 2, 3) 정수'      ← 정수 (JS 라 이게 이김)
```

받는 쪽은 이랬고요.

```ts
if (... && typeof f.medicalClass === 'string') {   ← 문자열일 때만
```

**AI 한테는 정수로 달라 하고, 앱은 문자열만 받았어요.** 종류가 자동으로 안 채워져요.

### 연쇄 효과가 더 나빠요
```ts
autofillExpiry(f.issuedDate, aiMedicalKey)   // aiMedicalKey 가 undefined
```
종류를 못 잡으면 **유효기간도 화면에 선택돼 있던 엉뚱한 종류로** 계산돼요.

| 종류 | 35세 기준 유효기간 |
|---|---|
| 제1종 | 12개월 |
| 제2종 | **60개월** |
| 제3종 | 48개월 |

**5배까지 벌어져요.** 신체검사는 앱에서 제일 중요한 커런시고요.

### 고친 것
- 스키마에서 중복 제거, 정수로 통일(문자열로 와도 받도록 안내 추가)
- 판정을 순수 함수 `medicalKeyFromAi()` 로 빼고 **정수·문자열 둘 다** 받게
- 테스트 4건 — 정수 · 문자열 · 알 수 없을 때 null · 종류 틀렸을 때 5배 차이

---

## ② 단독 항공영어 증명서가 통째로 무시됐어요

`if (!code) return out` 이 **148행**, 항공영어 블록이 **256행** 이었어요.
`licenceCode`(PPL/CPL/ATPL/MPL)가 없으면 그 전에 빠져나가요.

**단독 항공영어구술능력증명서를 올리면 제안이 하나도 안 만들어져요.**
초경량·경량과 **정확히 같은 구멍**이고, 이게 세 번째예요.

### 고친 것
조기 반환을 없애고 **자격증명 본체 블록만 감쌌어요.** 항공영어는 맨 아래에서 언제나 확인해요.

```ts
if (code) {
  ... 자격증명 · 한정 · 계기 · 교관 ...
}
// ── 항공영어 ── (본체가 아니어도 돈다)
```

폼에도 분기를 더해서, 항공영어 구분을 고르고 단독 증서를 올리면 **등급이 자동 선택**되고
만료일도 그 등급으로 계산돼요.

### 테스트가 옛 버그를 고정하고 있었어요
`it('licenceCode 가 없으면 아무것도 만들지 않는다')` — 이 테스트가 **버그를 정답으로**
박아 두고 있었어요. 정정했어요. 대신 "정말 아무것도 못 읽으면 빈 배열"을 따로 뒀고요.

그리고 블록을 옮겼더니 **제안 순서가 바뀌는 회귀**가 나서 기존 테스트 3건이 잡아 줬어요.
순서를 지키도록 구조를 다시 잡았어요.

---

## ③ 잡동사니 칸 제거

```js
ratings: '한정사항 문자열(예: 비행기 육상단발). 없으면 null',   ← 아무도 안 읽음
```

구조화된 `classRatings`·`ultralightRatings` 가 다 있는데 자유 텍스트 칸이 같이 있으면,
모델이 **거기에 몰아넣고 구조화 필드를 비울** 수 있어요. 실제로 초경량 1종이 이 칸에만
들어갔을 가능성이 있어요. 뺐어요.

---

## ④ 안 쓰는 필드 3개 — 남기되 이유를 적었어요

`documentKind` · `holderName` · `holderBirthDate` 는 앱이 소비하지 않아요.

**빼지 않았어요.** `documentKind` 는 모델이 문서 종류를 먼저 정하게 만들어 나머지 추출을
안정시키고, `holderBirthDate` 는 신체검사 유효기간(나이별)에 쓸 수 있는 값이에요.
지금은 계정 생년월일을 쓰고, 없으면 **50세 이상으로 보수적으로 가정**해요(방향은 안전해요).

주석에 "의도적으로 남긴 것이지 빠뜨린 게 아니다"라고 적었어요.

---

## 전수 대조표 — 나머지는 깨끗해요

| 구분 | AI 필드 | 스키마 | 제안 | 폼 자동선택 |
|---|---|---|---|---|
| 조종사 자격증명 | licenceCode | 있음 | 예 | 예 |
| 한정 | classRatings | 있음 | 예 | 예 |
| 계기비행증명 | instrumentRatings | 있음 | 예 | — |
| 조종교육증명 | flightInstructorRatings | 있음 | 예 | — |
| 항공신체검사 | medicalClass | 있음 | — | **예(고침)** |
| 항공영어구술능력증명 | eptaLevel | 있음 | **예(고침)** | **예(추가)** |
| 경량항공기 조종사 자격증명 | lsaRatings | 있음 | 예 | 예 |
| 초경량비행장치 조종자증명 | ultralightRatings | 있음 | 예 | 예 |
| 무선통신사 · 조종연습허가서 등 | — | — | — | 원시 칸만 채움(설계대로) |

계기·교관이 "폼 자동선택 —" 인 건 맞아요. 자격증명서에서 함께 나오는 항목이라
체크 목록으로 등록돼요.

---

## 검증

```
npx tsc --noEmit                  통과
npx eslint src --max-warnings=0   통과
npx vitest run                    178 통과 (168 + 신규 10)
npm run build                     통과
npx playwright test --list        20건
node --check netlify/functions/*  통과
```

프롬프트 변경은 **실제 AI 호출로 검증 못 했어요** — 컨테이너에 API 키가 없어요.

## 대표님이 하실 일

1. zip 덮어쓰기 → Commit → Push
2. **Netlify 함수가 바뀌었어요** — 배포 뒤 AI 읽기
3. 확인
   - **항공신체검사증명서**를 읽혀서 종류(1·2·3종)가 자동으로 잡히는지 ← 이번 핵심
   - 잡힌 종류로 **만료일이 맞게** 계산되는지
   - 자격증명서에서 조종교육증명이 **4개**로 나오는지(v2.9t)
