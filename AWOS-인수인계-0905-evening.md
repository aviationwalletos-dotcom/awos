# AWOS 인수인계 (2026-09-05 저녁) — 새 채팅 시작용

이 문서를 새 채팅 첫 메시지에 붙여 넣으면 이어서 작업할 수 있다. 한국어 해요체, zip 누적 배포 방식 그대로.

## 0. 한 줄 요약
**v1.2 배포 완료. E2E 12/12 통과(#25 기준). 실사용자 테스트(울진 5명, 2주) 시작 가능 상태.**
부채 3단계(`approval_requests`) + 보안 보강(schema14) + 기기 호환 보강까지 끝. 다음은 실사용자 피드백 대응과 TS 문서 갱신.

## 1. 프로젝트 기본
- React 19 + Vite + TS + Supabase + Netlify(GitHub main 자동 배포). PWA. pdf-lib.
- 저장소 github.com/aviationwalletos-dotcom/awos · Supabase vflyqnbdquaanpkvuinz · 관리자 aviationwalletos@gmail.com · 문의 awos.help@gmail.com
- 배포: Claude가 `/home/claude/awos` 수정 → 누적 zip → 사용자 덮어쓰기 → GitHub Desktop Fetch/Pull → Commit/Push. 충돌은 zip 쪽.
- **zip 파일명은 영문**(한글 이름은 다운로드 실패). 같은 이름 재사용 금지(옛 파일과 헷갈림) → `stage4f`, `stage4g`… 식으로 올린다.
- 컨테이너: npm은 `--cache /tmp/npmcache2 --legacy-peer-deps`, 브라우저 없음(E2E는 `--list`만). trace 분석은 `trace.zip` 안의 `*.network`(JSON lines)를 python으로 파싱하면 됨(이번 세션에서 두 번 결정적이었음).
- **최신 zip: `awos-v1.2-stage4e-0905.zip`** (push·배포·E2E 통과 확인됨)
- 실행된 SQL: schema11(이메일 로그인 여부) · 12(approval_requests, v2) · 13(전화번호 유니크) · 14(보안 보강) · 15(가입 확인에 연결 이메일·로그인 방법 포함). 모두 실행 완료.

## 2. 이 세션(9/5)에서 끝난 것
### 부채 3단계 — 서명·승인 전용 테이블 (`UPDATE-v55-부채3.md` 상세)
- `approval_requests` 1테이블 + RPC(`decide_approval_request`, `cancel_approval_request`, `list_approved_instructors`). 판정 후 불변, 판정자·시각 기록.
- 교관 승인 = (사용자 × 구분). 항공기 승인 뒤 경량·초경량 별도 신청 가능. 신청 전제 = 그 구분의 조종교육증명/지도조종자가 자격증 탭에 등록(신청서에 자동 첨부, 관리자 카드에 표시).
- 서명: 요청 시 기록 스냅샷+SHA-256 저장 → 교관 카드는 스냅샷을 표로 보고 서명(요약 텍스트로 속일 수 없음) → 서명 이력(N차 서명별 "그때 vs 지금" 표, 서버 행이라 삭제 불가). 교관 반려 가능(사유 학생에게 표시). 서명 요청함이 경량·초경량 탭에도 노출, 지금 보는 구분만 표시 + "다른 구분에 대기중 N건 보기 →".
- 관리자: 공용 `ApprovalQueuePanel`(교관/자격증·신체검사/증명서). 기본 '대기중' 서버 필터. 첨부는 눌렀을 때만.
- 옛 게시판 id 를 든 기록·자격증은 워처가 확인 후 연결 해제(다시 요청 가능). 서명 대기중 "요청 취소" 버튼.

### 보안·계정
- schema14: 요청자 이름·이메일 서버 강제(사칭 방지), 저장소 읽기 본인·관리자·서명 이미지만, 교관 목록 이메일 제거.
- 전화번호 1개 = 계정 1개(schema13, 인증 대기 계정 포함). 가입 전 이메일 확인이 연결 로그인 방법까지 판정해 "카카오로 가입돼 있어요 → 카카오로 로그인" 식 안내(schema15).
- 회원 탈퇴 3단계(체크박스 + 비밀번호 재입력 / 소셜 전용은 "탈퇴합니다" 문구).
- 소셜 로그인 콜백이 기관 계정도 개인 페이지로 보내던 것 → /dashboard. 계정정보 `?setup=1` 저장 후 튕김 제거.

### 안정성 (E2E가 찾아낸 진짜 버그들)
- GoTrue 클라이언트 다중 생성 → 잠금 경합 가능성 제거(`accessToken` 옵션, 인증 클라이언트 1개 재사용).
- 모든 Supabase 요청 20초(업로드 60초) 시간 제한. 계정 조회 실패 시 "연결이 불안정해요 · 다시 시도"(로그인으로 안 튕김).
- "JWT expired": 데이터 클라이언트 직접 사용 경로 전부 요청 전 토큰 갱신(`getFreshDataClient`).
- 목록 조회: 같은 필터 재조회 실패 시 마지막 목록 유지, 필터 바뀌면 이전 목록 폐기.
- lazy 모듈(PDF·페이지·xlsx) 새 배포로 파일 없음 → 1회 자동 새로고침.

### UI
- 기록 입력 폼·자격증 목록(3개 초과) 접기. 조종사 자격 템플릿 카드 제거. 주소 입력 제거. 보유 자격 구분 복수 선택 버그 수정.
- 경량·초경량 "응시경력" 탭 신설(자격증 다음). 회원 목록 여러 명 동시 펼치기.
- 역할별 자동채움 문구 정확화. 서명 완료 토스트.

### 기기 호환
- 카톡·네이버 인앱 브라우저 감지 → 구글 버튼 비활성 + 안내(안드로이드 "크롬으로 열기" intent, iOS Safari 안내·주소 복사). 카카오 로그인은 인앱에서도 됨.
- iOS 입력 확대 방지(16px), safe-area, `color-scheme: dark`, `<dialog>`·`AbortSignal.timeout` 폴리필, 좁은 화면 표 스크롤, 3열 그리드 → 폰 1열.

### E2E / CI
- 워크플로: `npm install`, Node 22, 시크릿 확인, **배포 커밋 = 테스트 커밋 대기(version.json, 불일치 시 실패)**, IPv4/IPv6 진단, 실패 시 네트워크·콘솔·연결 진단 메시지.
- 스펙: 실행마다 고유 표식, 진짜 기다림(`helpers.appears`; `isVisible({timeout})`는 즉시 판정이라 금지), 서명 패드 화면 중앙 스크롤 + hover 확인, 항공기 구분 명시 선택.
- 테스트 계정: 교관 wogjs1118@gmail.com(3구분 승인), 학생 = Secrets `E2E_STUDENT_EMAIL`(재생성됨, Auto Confirm).

## 3. 사용자가 다음에 할 일
1. 실사용자 테스트 시작 안내(공지방 초안은 이 세션 중반에 작성). 교관들: 계정정보 → 교관 승인 신청(구분별, 조종교육증명 먼저 등록).
2. 설정: Supabase 비밀번호 최소 8자, 관리자 구글 계정 2단계 인증.
3. 네이버 메일 인증 미수신 확인(Resend 로그) → 필요시 학생들에게 구글·카카오 로그인 안내.
4. 외부 공개 전: Supabase Pro(백업·저장소 100GB). 가입 하루 50명 넘으면 Resend 유료.
5. Google OAuth 동의 화면 브랜딩·인증 신청("vfly….supabase.co로 이동" 문구 개선).

## 4. 다음 작업(우선순위)
1. 실사용자 피드백 대응(기록 입력 "빠른 입력" 재설계 등)
2. TS 문서 갱신: 소개서에 서명 이력·스냅샷 해시·구분별 교관 승인 추가, 본인 서명 삭제·교관 확인 추가 / 질의서에 8.1.7.6·제77조② 추가 → 발송
3. 보류: 경량 커런시, DJI 로그 CSV, Remote ID, 비교관 기장 서명(제77조①2호), 초경량 유인 신체검사 카드

## 5. 법령 확인 결과 (변경 없음)
- 180일 = 운항기술기준 8.2.2 나 / 90일+야간 = 8.2.2 가·규칙 121조 / IFR 8.2.3 / 교관 규칙 125조 / 별표4·8 / 별지36호 / 8.1.7.6
- 경량항공기 법정 커런시 없음. 초경량 무인 세칙 9·10·13조, 별표2·3. 제77조 증명자(본인 서명은 증명 아님). 제78조 SIC 1/2.

## 6. 알아두면 좋은 것
- `account.id` = auth uuid, `account.user_id` = 이메일. 새 테이블은 uuid 기준.
- `signatureRequestPostId` / `certificateRequestPostId` / `approvalRequestPostId` 이름 유지, 값은 `approval_requests.id`. `signedRequestId` = 마지막 서명 완료 요청.
- 관리자 = `authorized_orgs`(`is_awos_admin()`). 소속 기관은 자기 신고(큐 필터용).
- 알고 받아들인 위험: 기록 자기 신고(종이와 동일), `contact_exists` 익명 호출로 가입 여부·방법 노출, 두 기기 동시 수정은 나중 저장이 이김, 오류 모니터링 없음.
- 콘솔 CSP inline script 오류는 Netlify 삽입 스크립트라 기능 영향 없음.
- 사용자(우혁준)는 초보 개발자·조종사. 설명은 짧고 결론 먼저, 법 근거는 조문 번호로.
