# UPDATE v42 (2026-09-04)

1. **[BUGFIX] 초경량 증명서 PDF 표가 페이지 밖으로** — 열 폭 비율 합이 103.8%라 오른쪽 끝(지도조종자 서명)이 잘림. 열 폭을 합계로 정규화(별지 36호도 동일 보호).
2. **초경량·경량 첫 화면 2열** — 자격 현황 카드가 가로 전체를 차지하던 것을 조종사 화면처럼 반으로. 오른쪽 칸: 초경량은 **내 기체 요약**(기종·종·인증 D-day, "관리" → 기록 입력 탭), 경량은 **최근 비행 3건**.
3. **서명 요청함 날짜 입력** — 값이 비면 칸이 빈 것처럼 보이던 문제. 앞에 "비행 일자" 라벨.
4. **[BUGFIX] 카카오 KOE205** — 이메일 플래그가 꺼져 있을 때는 scopes 파라미터를 아예 보내지 않도록 원복(카카오 앱 기본 동의항목으로 요청 = 예전에 잘 되던 상태). 명시 scope가 동의항목과 조금이라도 어긋나면 KOE205가 나기 때문. 비즈 앱 전환 후 `VITE_KAKAO_EMAIL_SCOPE=true`를 켜면 그때만 `profile_nickname account_email`을 명시.
5. 경량항공기 커런시 — 아래 답변 참고(법령 확인 후 반영).
