# UPDATE v1.4h — 엑셀 가져오기 보강 (2026-09-10)

## 무엇이 달라졌나
미국 훈련 기록·해외 로그북 엑셀에서 **날짜와 시간이 잘못 읽히던 문제**를 고쳤어요.

### 1. 시간 — 분 단위 인식
- `1:30`, `1h30m`, `1시간 30분`, `90분`, `90 min` 을 읽어요. (예전엔 전부 빈 칸 처리)
- 순수 숫자만 있는 파일은 열 전체를 보고 판정해요. **25 이상 정수만 있으면 분**으로 읽어요(한 번 비행이 25시간일 수 없으니까요). 소수가 하나라도 있으면 시간이에요.
- 값은 앱 규약대로 0.1시간 단위로 반올림해요. 100분 → 1.7.
- **착륙 횟수·계기접근 횟수는 시간 변환을 하지 않아요.**

### 2. 날짜 — 미국식
- `7/1/2025`, `7/1/25`(두 자리 연도), `Jul 1, 2025`, `01 Jul 25` 를 읽어요.
- `03/04/2025` 처럼 월·일 구분이 안 되는 값은 **파일 전체를 훑어** 순서를 정해요. 어느 한 행에 13 이상이 있으면 그걸로 확정해요.
- 끝까지 모호하면 미국식(월/일/년)으로 읽고 **주황색으로 알려줘요.**
- 엑셀 날짜 일련번호(`45839`)도 읽어요.

### 3. 화면
- 미리보기 위에 **"읽기 방식"** 박스: 날짜 순서 / 시간 단위 직접 변경 가능.
- 미리보기 날짜·블록타임에 `7/1/25 → 2025-07-01` 처럼 변환 결과를 같이 보여줘요.

## 파일
- 새로 만듦: `src/lib/legacyImportParse.ts` (파싱 규칙), `src/lib/legacyImportParse.test.ts` (23건)
- 수정: `src/components/logbook/LegacyExcelImport.tsx`

## 검증
`tsc` 통과 · `eslint --max-warnings=0` 통과 · `vitest` **90건**(67 → 90) 통과 · `vite build` 통과 · `playwright --list` 17건

## 삭제할 파일
없음.

---

# UPDATE v1.4i — 로그인 후 헤더 정리 (2026-09-10)

## 고친 버그
**관리자 계정에 "로그북으로"가 보이던 문제.** 관리자(기관) 계정은 로그북 화면이 없어요. 계정 페이지의 버튼·링크가 전부 `/dashboard`로 가고, 글자도 "대시보드로 →"로 바뀌어요.

## 바뀐 화면
로그인한 뒤에는 소개 페이지로 나갈 일이 없어서, 나가는 링크를 없애고 **로고를 홈 버튼으로** 바꿨어요.

| 화면 | 전 (왼쪽) | 후 (왼쪽) |
|---|---|---|
| 로그북 | ← 소개 페이지 | **Aviation Wallet OS** → 누르면 기록 탭 맨 위 |
| 관리자 대시보드 | ← 홈으로 | **Aviation Wallet OS** → 누르면 인원 탭 맨 위 |
| 계정 | ← 로그북으로 | **Aviation Wallet OS** → 누르면 로그북(관리자는 대시보드) |

- 오른쪽에 중복으로 있던 "Aviation Wallet OS" 글자는 없앴어요(로고가 왼쪽으로 갔으니까요).
- 계정·로그아웃 버튼을 화면 끝에서 살짝 안쪽으로 옮겼어요(`sm:pr-10`).
- **관리자 대시보드에 계정·로그아웃 버튼을 새로 넣었어요.** "홈으로"를 없애면 관리자가 계정 화면에 갈 길이 사라지기 때문이에요.

## 파일
`src/pages/LogbookPage.tsx` · `src/pages/DashboardPage.tsx` · `src/pages/AccountPage.tsx`

## 검증
`tsc` · `eslint` 통과 · `vitest` 90건 · `vite build` 통과 · `playwright --list` 17건

## 삭제할 파일
없음.

---

# UPDATE v1.4j — 주간 시간 계산 제안 (2026-09-10)

## 무엇이 달라졌나
비행경력증명서 폼의 **주간(시간)** 칸 아래에 계산 버튼이 생겨요.

```
총 69.9 − 야간 3.4 = 66.5 넣기
```

증명서에는 야간만 적히고 주간은 생략되는 경우가 많아요(나머지가 전부 주간이니까요). 손으로 빼는 수고를 없앴어요.

## 뜨는 조건
**총 비행시간과 야간이 둘 다 채워져 있고, 주간이 비어 있을 때만.** 주간에 값이 들어가면 버튼은 사라져요. 계산 결과가 0 이하면 안 떠요.

## 왜 AI 가 아니라 앱이 계산하나
AI 프롬프트에 "주간 = 총 − 야간으로 계산해라"를 넣지 **않았어요.** 지금 정책이 "문서에 없는 값은 비운다"인 이유가 있어요.

- 관리자가 증명서와 대조 승인할 때, 증명서에 없는 66.5가 채워져 있으면 무엇을 근거로 맞다고 할지 애매해져요.
- AI 가 계산까지 하면 **어디까지가 읽은 값이고 어디까지가 만든 값인지 구분이 사라져요.**

그래서 계산은 앱이 하고, 넣을지는 사용자가 누르고, 값은 사용자가 넣은 것으로 남아요. 정책은 그대로예요.

## 파일
`src/components/logbook/FlightExperienceCertificateForm.tsx`

## 검증
`tsc` · `eslint` 통과 · `vitest` 90건 · `vite build` 통과 · `playwright --list` 17건

## 삭제할 파일
없음.

---

# UPDATE v1.5 — 자격증 한 장으로 여러 카드 · TS 문구 정리 · AI 전송 동의 (2026-09-10)

## 1. 자격증 사진 한 장 → 한정·계기·교관·항공영어까지
CPL 사진을 올리고 "사진에서 읽어오기"를 누르면 자격증명서의 한정사항(XII)·특기사항(XIII)에서
- 종류·등급(비행기/헬리콥터 × 육상단발/다발/수상)
- 계기비행증명
- 초급·선임 조종교육증명
- 항공영어구술능력 등급(VALID UNTIL 있으면 그대로, 없으면 제99조③으로 4등급 3년·5등급 6년·6등급 영구 계산)

을 뽑아 **"이 사진에서 같이 찾은 자격 N개"** 체크 목록으로 보여줘요. 이미 등록된 것은 "이미 등록됨"으로 기본 체크가 꺼져요. 등록하면 체크한 것이 같은 사진으로 각각 등록·인증 요청돼요. '한정'은 자격증명 카드 안에 붙어요.

- 새 파일: `src/lib/certificateSuggestions.ts` + 테스트 9건
- `read-document.mjs`: `licenceCode`·`instrumentRatings`·`instructorRatings`·`eptaLevel`·`eptaValidUntil`
- **연결 누락 수정**: 폼이 `extras`를 넘기는데 `CertificatesTab`이 안 받고 있었어요 → 이제 저장돼요.
- 제목: "자격증 등록" → **"자격증 · 한정 등록"**

## 2. TS 문구 제거
- 랜딩 `content.ts`: "한국교통안전공단(TS) 자격 연동 — 준비 중" 삭제
- 자격증 탭 하단 `TsIntegrationCard` 렌더 제거(컴포넌트 파일은 남김). 공단과 협의된 게 없는데 "연동 준비 중"이라 쓰면 공단 이름을 앞세운 것처럼 보여요.

## 3. AI 전송 동의(개인정보 국외 이전)
"사진에서 읽어오기" 버튼 위에 체크박스: *사진은 AI 가 읽기 위해 해외 서버(Anthropic, 미국)로 전송돼요. 읽는 데만 쓰이고 모델 학습에는 쓰이지 않아요.* 동의 전엔 버튼이 안 눌려요. 기기당 한 번 기억(`localStorage awos_ai_transfer_consent`), 철회 링크 있음.
→ **개인정보처리방침에도 같은 내용을 넣어야 해요**(국외 이전 항목: 이전받는 자, 국가, 목적, 항목, 보유기간).

## 검증
`tsc` · `eslint` 통과 · `vitest` **99건** · `vite build` 통과 · `playwright --list` 17건

## 삭제할 파일
없음.

---

# UPDATE v1.5a — 법령 API 실패 원인 표시 (2026-09-10)

법령 관리 탭의 "API 에서 못 찾음"이 원인을 안 보여줬어요. 서버 함수는 `note`로 이유를 돌려주는데 화면이 버리고 있었어요.
- 함수: JSON 이 아닌 응답이면 HTTP 상태와 응답 앞 120자를 note 에 담음(OC 미승인이면 HTML 안내 페이지가 옴). 검색 0건이면 totalCnt 표시.
- 화면: 배지에 note 를 같이 표시.
→ "최신 여부 한 번에 확인"을 다시 누르면 배지에 원인이 보여요.

파일: `netlify/functions/check-regulations.mjs`, `src/components/dashboard/RegulationPanel.tsx`. 삭제할 파일 없음.

---

# UPDATE v1.6 — 탈퇴해도 서명 증거는 남긴다 · 탈퇴 전 CSV 백업 (2026-09-10)

## 결정 (2026-09-10)
- **원칙: "내 기록에 적힌 상대방 이름은 남는다."** 종이 로그북과 같다.
- 근거: 개인정보보호법 제15조①6호(정당한 이익) — 교관의 교관시간 경력 증명에 학생 이름이 필요.
  조종사 개인 로그북의 법정 보존 기간은 없음(제56조③ 15개월은 사업자 의무). 그래서 "보존 의무"가 아니라 "정당한 이익"으로 간다.
- 유예 없이 **즉시 삭제**(테스트 단계). 유예가 필요해지면 함수를 "예약"으로 바꾼다.
- **개인정보처리방침에 넣을 문장**: "서명 기록에 포함된 이름은 상대방의 비행경력 증명을 위해 탈퇴 후에도 남습니다."

## 무엇이 지워지고 무엇이 남나
| 지워짐 | 남음 |
|---|---|
| 계정, 이메일·전화·생년월일·주소 | 서명 요청 행(승인·반려·취소 이력, 요청자 이름, 스냅샷·해시) |
| 본인 로그북 전체 | 교관 손글씨 서명 이미지 |
| 본인 자격증·증명서와 사진, 그 인증 요청 | 교관 승인 기록(자격번호 = 서명 자격의 증거) |
| 아직 서명 안 된 요청 → "취소"로 바뀜 | 화면엔 "탈퇴한 사용자" 표시 |

## SQL — `supabase/schema17-withdrawal-keeps-signatures.sql` (실행 필요)
1. `approval_requests.requester_id`: cascade → **set null**, nullable
2. `requester_deleted_at` 컬럼
3. `delete_my_account()` 재작성: 사진 붙은 인증 요청 삭제 → 대기 요청 취소 → 이메일 비우고 탈퇴 시각 기록 → 내 파일 삭제(서명 이미지 제외) → auth.users 삭제

## 화면
- 탈퇴 흐름: 진행하기 → **"비행기록 N건을 CSV로 저장할까요?"** (저장하기 / 저장 없이 계속 / 취소) → 본인 확인 → 삭제. 기록 0건이면 백업 단계 건너뜀.
- 안내 문구: "다른 사람의 로그북에 남은 서명 기록은 그대로 남아요"
- 관리자 큐·교관 서명함: 요청자 옆 "탈퇴한 사용자" 배지

## 파일
`supabase/schema17-…sql`(새) · `src/pages/AccountPage.tsx` · `src/lib/approvals/types.ts` · `src/components/dashboard/ApprovalQueuePanel.tsx` · `src/components/account/InstructorSignatureInboxSection.tsx`

## 검증
`tsc` · `eslint` 통과 · `vitest` 99건 · `vite build` 통과 · `playwright --list` 17건. 삭제할 파일 없음.

---

# UPDATE v1.6b — 법령 API: http 로 호출 · 운항기술기준은 행정규칙(admrul)로 검색 (2026-09-10)
- https 로 부르면 법령정보센터가 리다이렉트하며 검색어를 떨어뜨려 "필수 입력값이 존재하지 않습니다"가 왔음 → http 우선, 실패 시 https.
- 운항기술기준(국토교통부 고시)은 행정규칙이라 `target=admrul` 로 찾음. 응답 필드가 다름(AdmRulSearch.admrul / 행정규칙명 / 발령일자 / 행정규칙상세링크).
  `regulations.ts` 에 `lawGoKrTarget: 'admrul'`, 검색어 "고정익항공기를 위한 운항기술기준".
- 공단 운영세칙(초경량)은 법령정보센터에 없음 → 배지에 "공단 내부 규정 — API 범위 밖" 표시, 링크로 수동.
파일: `check-regulations.mjs`, `RegulationPanel.tsx`, `regulations.ts`. 삭제 없음.

---

# UPDATE v1.7 — 피드백 반영 1·2·3·6·7 (2026-09-10)

1. **AI 안내문에서 코드 이름 제거** — 프롬프트에 notes 작성 규칙 추가(필드 이름·null·JSON 용어 금지, 화면의 한국어 칸 이름으로). `read-document.mjs`
2. **자격증 중복 등록 차단** — 같은 구분에서 자격번호가 같거나(번호 있을 때), 이름·종류·등급이 모두 같으면(번호 없을 때) 저장 막고 안내. `CertificateForm.tsx`
   · E2E 참고: 03-certificate 는 끝에 삭제하므로 영향 없음. 중간에 실패해 남으면 다음 실행에서 중복 오류가 날 수 있음 → 계정 재생성으로 해결.
3. **AI 읽기 하루 10회/계정** — `supabase/schema18-ai-quota.sql`(실행 필요): `ai_usage` 테이블 + `consume_ai_quota()`. 함수가 AI 호출 전에 소비, 초과 시 429 "하루 10회까지". 결과 패널에 "오늘 3/10회 사용" 표시. SQL 미실행이면 한도 없이 통과(기능 중단 방지).
6. **계정 화면 "최근 로그인"** — `auth.users.last_sign_in_at` 로 채움. 날짜 표기에서 초 제거.
7. **내 데이터 전체 내보내기(JSON)** — 계정 화면에 새 섹션. 로그북·자격증·내가 낸 서명/인증 요청과 결과(서명자·일시·스냅샷·해시)·계정 요약. `src/lib/fullExport.ts`

## 검증
`tsc` · `eslint` 통과 · `vitest` 99건 · `vite build` 통과 · `playwright --list` 17건. 삭제할 파일 없음.
