# AWOS 배포 안내서 (따라 하기)

이 문서는 코드를 인터넷에 올려 실제 링크를 만드는 순서다.
코드·설정은 이미 다 되어 있으니, **가입하고 파일 올리고 버튼 누르는 것**만 하면 된다.
전체 30분 정도. 순서대로만 하면 된다.

---

## 준비물

- 이 폴더(압축을 푼 AWOS 소스 전체)
- 지메일 계정 (GitHub·Netlify 가입에 사용)

---

## 1단계. GitHub 계정 만들기 (5분)

GitHub는 코드의 원본 보관소다. 여기 코드를 올려야 배포가 시작된다.

1. https://github.com 접속 → 오른쪽 위 **Sign up**
2. 이메일(지메일 주소 입력) → 비밀번호 → 사용자명(username) 순서로 입력
   - 사용자명은 영문/숫자, 공개되니 적당한 걸로 (예: `woohyukjun`, `awos-team`)
3. 이메일로 온 인증 코드 입력 → 가입 완료
4. 요금제는 **Free** 선택

---

## 2단계. 저장소(repository) 만들기 (3분)

저장소는 코드를 담는 폴더다.

1. 로그인 후 오른쪽 위 **+** 아이콘 → **New repository**
2. 입력:
   - **Repository name**: `awos` (또는 원하는 이름)
   - **Public / Private**: **Private** 선택 (코드를 비공개로 — 권장)
   - 나머지(README, .gitignore 등)는 **아무것도 체크하지 말 것** (이미 파일에 있음)
3. **Create repository** 클릭
4. 다음 화면이 나오면 그대로 두고 3단계로 (여기 주소를 곧 쓴다)

---

## 3단계. 코드 올리기 (10분)

가장 쉬운 방법은 **웹 화면에 드래그 앤 드롭**이다.

1. 방금 만든 저장소 화면에서, 파란 링크 중 **uploading an existing file** 클릭
   - 안 보이면 저장소 주소 뒤에 `/upload/main` 을 붙여 접속
2. 이 폴더 안의 **모든 파일과 폴더를** 화면의 점선 박스 안으로 드래그
   - ⚠️ 주의: `node_modules` 폴더는 올리지 말 것 (매우 크고 불필요). 이 폴더가 보이면 빼고 올린다. (원래 이 배포 패키지에는 없음)
   - `src`, `index.html`, `package.json`, `netlify.toml` 등이 다 들어가야 한다
3. 파일이 다 올라오면 아래 **Commit changes** 버튼 클릭
4. 잠시 후 저장소에 파일들이 보이면 성공

> 파일이 많아 드래그가 번거로우면, `src` 폴더와 나머지 파일들을 두세 번에 나눠 올려도 된다.

---

## 4단계. Netlify로 배포하기 (5분)

Netlify가 GitHub 코드를 읽어 자동으로 사이트를 띄운다.

1. https://netlify.com 접속 → **Sign up** → **Sign up with GitHub** 선택
   - 방금 만든 GitHub 계정으로 바로 연결된다
2. 로그인 후 **Add new site** → **Import an existing project**
3. **Deploy with GitHub** 선택 → (권한 요청이 나오면 승인)
4. 저장소 목록에서 방금 만든 **awos** 선택
5. 빌드 설정 화면이 나온다. 아래처럼 되어 있는지 확인 (대부분 자동으로 채워짐):
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - (netlify.toml에 이미 있어서 자동 인식된다. 비어 있으면 위 값을 직접 입력)
6. **Deploy** 클릭 → 1~2분 기다리면 빌드 완료
7. 화면 위에 `https://무작위이름.netlify.app` 형태의 **주소가 생긴다** → 이게 우리 사이트!

---

## 5단계. 확인 (중요)

생성된 `.netlify.app` 주소를 **폰으로 열어본다.**

- 랜딩페이지가 뜨는지
- 로그인 → 로그북에서 **비행시간이 노트북과 똑같이 나오는지** (동기화 버그 수정 확인)
- 기존 계정(rlrhks@naver.com 등)으로 로그인해 기존 기록이 그대로 보이는지

여기까지 되면 1단계(프론트 독립 배포) 완료다.

---

## 다음 단계 (나중에)

- **주소 예쁘게**: `awos.co.kr` 같은 자체 도메인 구매 후 Netlify에 연결
- **완전 독립**: 백엔드(로그인·데이터)를 aiapp에서 Supabase로 이전
- **앱 출시**: Capacitor로 안드로이드 앱 포장

이 단계들은 준비되면 별도로 안내한다.

---

## 문제가 생기면

- **빌드 실패(Netlify에서 빨간 X)**: 화면의 로그를 캡처해서 공유하면 원인을 짚어준다.
- **사이트는 뜨는데 로그인/기록이 안 됨**: 백엔드 프록시 설정 문제일 수 있으니 공유.
- 대부분의 문제는 파일이 일부만 올라갔을 때 생긴다. `package.json`, `netlify.toml`, `src` 폴더가 저장소에 다 있는지 먼저 확인.
