# AWOS 인수인계 (2026-09-13) — 새 채팅 첫 메시지에 붙이기

한국어 해요체. 결론 먼저, 지시는 하나씩. zip 누적 배포.
**채팅은 한 번에 하나만.** 새 창을 열면 이전 창은 닫아요(두 창이 같은 폴더를 쓰면 꼬여요).
이 문서 + 최신 zip(`awos-v2_9f-0913.zip`)을 같이 올려 주세요. 상세 이력은 zip 안 `UPDATE-v56.md`, 기능 전체 설명은 `docs/feature-guide-0913.md`.

## 0. 지금 상태 한 줄
**v2.9f.** 실사용자 테스트(5명) 진행 중. 대표가 "앱 개발은 여기까지" 선언했고, 남은 건 TS 미팅과 운영 준비. 검증 전부 통과(tsc·eslint·vitest 109·build·E2E 20).

## 1. 기본
- React 19 + Vite + TS + Supabase + Netlify(main 자동 배포). 저장소 github.com/aviationwalletos-dotcom/awos · Supabase `vflyqnbdquaanpkvuinz` · 사이트 aviationwallet.com
- 관리자 aviationwalletos@gmail.com · 문의 awos.help@gmail.com
- 배포: Claude 가 `/home/claude/awos` 수정 → `awos-<버전>-<MMDD>.zip` → 대표가 덮어쓰기 → GitHub Desktop Push → Netlify 자동 배포.
  - **zip 은 파일 삭제를 전하지 못함** → 지운 파일은 `DELETE-LIST-<날짜>.md` 로 따로.
  - **파일명은 영문만.** 한글 파일명이 Netlify 빌드를 깨뜨린 적 있음(2026-09-10).
- 검증 순서: `npx tsc --noEmit` → `npx eslint src --max-warnings=0` → `npx vitest run`(109) → `npm run build` → `npx playwright test --list`(20). 컨테이너엔 브라우저 없음.
- npm 설치: `npm install --no-audit --no-fund --cache /tmp/npmcache2 --legacy-peer-deps`
- 파이썬 문자열 치환이 sed 보다 안전(한글·특수문자 많은 TSX).

### 환경변수(Netlify, 모두 설정 완료)
`ANTHROPIC_API_KEY`(secret 체크) · `SUPABASE_URL` · `SUPABASE_ANON_KEY`(`sb_publishable_…`) · `LAW_GO_KR_OC`(= `aviationwallet`) · 선택 `VITE_SENTRY_DSN`(미설정 — 넣으면 오류 모니터링 켜짐)

### SQL — schema1~23 **전부 실행 완료**
최근: schema17(탈퇴 시 서명 보존) · 18(AI 하루 10회) · 19(조문 해시) · 20(교관 확인 endorsement) · 21(첨부 여러 장) · 22(탈퇴 스토리지 오류 수정) · 23(탈퇴 교관 목록 제외)

## 2. 9/9~9/13 에 한 일 (v1.4g → v2.9f)
- **엑셀 가져오기**: `1:30`·`90분`·분 단위 열 자동 감지, 미국식 날짜·두 자리 연도·엑셀 일련번호, "읽기 방식" 박스
- **헤더**: 로고가 홈 버튼(로그북·대시보드·계정). 관리자에게 "로그북으로" 뜨던 버그 수정
- **자격증**: 사진 한 장에서 한정·계기·초급/선임 교관·항공영어까지 추출 → 체크 목록으로 함께 등록. 중복 차단. 첨부 여러 장(최대 4). **이미 등록된 자격증이면 "기존 자격증에 추가" 모드**(v2.9f). 조종교육증명은 **등급별**(육상단발/다발)
- **AI 읽기**: 국외 이전 동의, 하루 10회/계정, 신체검사·무선·조종연습허가서까지 확장, 여러 장 동시 읽기
- **비행경력증명서 AI**: 별지 36호 "계" 행 19칸을 AI 가 **그대로 베끼고 앱이 합산·검산**(주간+야간=소계, 기장+부조종사+학생=소계, 교관≤기장). 교관↔학생 열 바뀜 자동 교정. 실제 증명서 3종으로 단위 테스트
- **교관 전자서명**: 서명 시점 스냅샷 + SHA-256, 서명 후 수정 시 서명 해제, 자기 서명 차단(서버). **교관 확인(Endorsement)도 교관 전자서명으로**(schema20)
- **해외 기록**: `foreignRecord` 자동 판정 + 체크박스. 국내 교관 서명 대상 제외, 미서명 필터 제외, PDF 비고에 표기
- **탈퇴 정책**: "내 기록에 적힌 상대방 이름은 남는다". CSV 백업 단계, 즉시 삭제, "탈퇴한 사용자" 배지
- **법령 관리**: 법령 5건 + **조문 16개·별표 3개 단위 변경 감지**(본문 API + Referer, 별표는 PDF 해시), 시행 예정 개정 알림, **개정 대응 매뉴얼 복사 버튼**
- **법령 원문 대조**: 본문 조문·별표 4·8·18 전부 확인. **별표 18 "7일 35h" 삭제(근거 없음), 30일→28일 정정**
- **법적 빈틈 11개**: 한정 없는 등급 GO 제외, 8.2.4 회복 안내, 야간 크로스컨트리 칸, FTD·BATD 착륙 제외, 자가용 승무시간 "참고" 등
- **기타**: 내 데이터 JSON 내보내기, 최근 로그인 표시, 관리자 큐 검색+5건 페이지, Sentry 스텁, 탈퇴 E2E, 개인정보처리방침(국외이전·탈퇴 보존), ⓘ 40개 전수 검토

## 3. 법령 사실 (원문 확인 완료)
- **제76조② 는 2026-11-13 삭제 → 제39조의4 신설**(소지 의무 이동). 같은 날 제44조②3호 신설. **그날 화면·문서의 "제76조②"를 바꿀 것** — 법령 관리 탭이 노란 띠로 알려 줌
- 제77조①: 비행이 끝날 때마다 기장·감독자 증명 / ②나: 기장 본인 경력은 조종교육증명+제125조 경험 있는 교관이 증명(= 관리자가 교관 승인 때 조종교육증명 보는 근거) / ②다: "국토교통부장관이 고시하는 사람" ← **TS 질의 항목**
- 8.1.7.6 나 단서: 항공사업 조종사는 사업자 전산 출력물로 로그북 대체 가능 ← Q1 논거
- 조종사 개인 로그북 법정 보존기간 없음(제56조③ 15개월은 사업자 의무)
- 8.2.3 = 접근 6회 **+ 계기비행 6시간**. 8.2.4 마(2인조종 SIC 12개월 3회)는 미구현·범위 밖
- **별지 36호 규칙**(탈론 798건 ↔ 오재헌 증명서 전 항목 일치로 확정): 기장 = PIC 전부(교관 비행 포함) / 교관조종사 = Instructor(참고 열, 소계에 안 더함) / **학생조종사 = Dual O·PIC X** / 소계 = 기장+부조종사+학생 / 야간 야외 = min(야간, 야외) / 모의비행 = 모의계기, 기타 = FTD 총시간
  - **Dual 전체는 한국 증명서에 없음**(자격증 후 교육은 기장에 포함) → 공란 + 상한(소계−교관−부조종사) 안내. 미국식(AeroGuard)은 "Dual Received" 값 그대로
  - 미해결: 야외 합계 탈론 344.3 vs 증명서 347.1 (2.8h) — 원본 표기 차이, 학교에 산정 기준 문의 필요

## 4. TS 미팅 (9/14~22, 김상우 책임연구원)
- 문서: `AWOS-기능소개-v2.pptx`(14장, K-EPL 생성원 슬라이드 포함) · `AWOS-질의-v4.pptx`(질의 7건)
- **발송 여부 미확인 — 대표에게 확인할 것**
- 미결: Q8(탈퇴 조종사 서명 기록 K-EPL 보존) 추가 여부
- 미팅에서 꺼낼 것: 제77조②다 고시 대상자 / 초경량 운영세칙 개정 알림 방법 / 해외 기록 K-EPL 반영 / 야외비행 산정 기준(KAU 에도)

## 5. 남은 일 (우선순위)
1. **TS 미팅** 및 문서 발송
2. **Supabase Pro 결제($25/월)** — 자동 백업 없음. 7일 유휴 시 프로젝트 정지. E2E flaky(01-login 콜드 스타트)도 같이 해결될 가능성
3. 아이폰 검증(채지훈 씨) — 갤럭시 크롬만 확인함
4. 교관 확인(Endorsement) 실제 서명 흐름 테스트 — schema20 실행은 했으나 실사용 미확인
5. Sentry DSN 설정 / 네이버 서치어드바이저 코드 / 오프라인 실기 확인
6. 증명서 다른 서식 검증(3종만 확인) — 검산이 잡아 주지만 완전하지 않음
7. 사업자 등록(창업보육센터 입주 완료) · 개인정보처리방침 최종 점검
8. 보류: 탈론 자동 연동, 자격증 자동 인증(TS API), 경량 커런시, DJI CSV, Remote ID

## 6. 결정·정책 (바뀌지 않는 것)
- 이월 기록 승인 주체는 **관리자(대표)**. "기관·학교가 확인" 표현 금지. 기관과 협의되면 그때 기관이 역할을 맡음
- **AI 는 문서에 없는 값을 만들지 않음.** 계산은 앱, 판단은 사람. 법령 값 자동 수정 없음(감지까지만)
- 탈퇴: 상대방 기록 속 이름은 남음(개인정보보호법 제15조①6호). 즉시 삭제, 유예 없음
- 해외 기록은 국내 교관 서명 대상 아님. 관리자 증명서 대조 승인으로 갈음
- **안내 문구는 한 줄 + ⓘ(InfoTip).** 회색 설명 줄을 늘리지 않음. 의미 없으면 삭제
- 화면에 버전 번호·지명(울진 등) 넣지 않음. 조문은 실제 번호로만
- 카드덱 유지, 기록 폼 접지 않음, 승무시간 카드는 모두에게(자가용은 "참고")
- GO 배지: 하나라도 가능하면 GO, 단 한정 없는 등급은 제외

## 7. 알아두기
- `account.id` = auth uuid, `account.user_id` = 이메일. `approval_requests` 가 서명·승인 단일 테이블(`requester_id` 는 탈퇴 시 set null)
- `dayLandings: undefined = 기재 없음`(0 아님). `conditions.nightCrossCountry` 는 9/10 이후 기록만
- 관리자 = `authorized_orgs`(`is_awos_admin()`). 자기 서명은 서버에서 차단
- **법령정보센터 API**: 목록·본문·별표 모두 `Referer: https://aviationwallet.com/` 헤더 필요. 주소창 직접 호출은 실패가 정상. 행정규칙 한글주소는 띄어쓰기 그대로. 본문 조회는 IP 검증 요구 → Netlify 에서 Referer 로 통과
- **승무시간 계산에서 이월 기록 제외**(v2.9e) — 합계 963h 가 하루치로 잡혀 NO-GO 나던 문제
- **교관 목록**은 `list_approved_instructors()` RPC. 탈퇴자 제외(schema23)
- 알고 받아들인 위험: 기록 자기 신고, 두 기기 동시 수정 후승, 1종 6개월 예외를 "상업 40세↑" 전부에 보수 적용, API 키 스크린샷 노출(월 한도로 방어)
- **대표(우혁준)는 조종사이자 초보 개발자.** 항공 실무는 대표가 정확함 — 학생조종사/Dual 구분처럼 Claude 가 틀린 걸 대표가 여러 번 바로잡았음. 법·실무 판단은 대표 말을 우선하고, 근거를 물어 확인할 것. 설명은 짧게, 법 근거는 조문 번호로, "딱 하나만"

## 8. 규모
소스 222개 · SQL 23단계 · 자동 테스트 129건(단위 109 + E2E 20) · 응시경력 요건 118개 · 법령 감시 조문 16 + 별표 3 · ⓘ 40개
